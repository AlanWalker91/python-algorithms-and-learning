# 《AI Agent 测试与测开岗面试复习笔记》

> 面向岗位：AI Agent 测试开发 / SDET（中高级）
> 使用方式：面试前按「知识地图 → 模块笔记 → 速答清单」顺序复习；每个问题先尝试口头复述，再对照答案。
> 标注约定：**【修正】** 表示对原始资料错别字／术语错误的更正；**[补充]** 表示原资料未提供、由通用工程共识补全的内容。

---

## 第一部分　面试知识地图

| 层级 | 模块 | 核心问题 | 高频词 |
| --- | --- | --- | --- |
| L0 基础设施 | 大模型基础与训练推理 | LLM 如何训练、如何推理、如何扩展 | Transformer、注意力、预训练、后训练、RLHF、KV Cache、Scaling Law |
| L0 数据底座 | 数据工程与 DCAI | 数据如何支撑模型迭代 | DCAI、ETL/ELT、数据清洗、EDA、数据飞轮 |
| L1 应用形态 | AI Agent 基础与架构 | Agent 是什么、怎么构建、怎么协作 | ReAct、CoT、Reflection、MCP、A2A、Multi-Agent |
| L2 交互层 | Prompt Engineering | 如何写好、写好之后如何优化 | 四要素、分隔符、Few-shot、多模态 |
| L2 上下文层 | Context Engineering | 有限上下文窗口如何管理 | 压缩、摘要、过滤、隔离、压缩时机 |
| L2 知识层 | RAG、微调与知识增强 | 知识从哪来、幻觉怎么治 | 语义分片、向量化、混合检索、重排 |
| L3 运行时层 | Harness Engineering | 模型之外如何框住行为 | 认知框架、能力边界、标准工作流 |
| L4 评测层（本岗核心） | Agent 评测 vs LLM 评测 | 系统级能力怎么测 | NL2API/NL2SQL/NL2Code、轨迹评测、成功率 |
| L4 评测层（本岗核心） | LLM 大模型测评体系 | 测评怎么成体系落地 | 质量属性、裁判模型、绝对/相对测评、数据回流 |

**岗位定位一句话**：测开工程师在 Agent 时代的核心价值 = 把「不确定的模型输出」变成「可度量、可回归、可持续优化」的工程对象——评测集是抓手，裁判模型是放大器，数据回流是飞轮。

---

## 第二部分　模块化笔记

---

## 模块一　大模型基础与训练推理

### 1.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
| --- | --- | --- |
| 人工智能（AI） | 让机器模拟人类智能行为的技术总称 | 是最大范畴，包含机器学习、NLP、CV 等 |
| 传统 NLP | 基于规则／统计方法处理自然语言的技术路线 | 任务割裂（分词、NER、分类各自建模），泛化弱 |
| 大模型 | 参数规模巨大（通常十亿级以上）、基于大规模语料训练的神经网络 | 「大」带来涌现能力（Emergent Ability） |
| 大语言模型（LLM） | 以语言建模为目标的大模型，通过预测下一个 Token 学习语言与世界知识 | 大模型 ⊂ AI；LLM 是当前大模型的主流形态 |
| Transformer | 基于自注意力机制的序列建模架构，LLM 的统一底座 | 并行度高、长距离依赖建模强，替代 RNN/LSTM |
| 注意力机制 | 计算序列中各位置之间的相关性权重，动态分配「关注」 | Q·K 算相关性，加权 V；Self-Attention 是核心 |
| 词嵌入（Embedding） | 把离散 Token 映射为稠密向量，承载语义信息 | 语义相近的词向量距离近；也是 RAG 检索的基础 |
| 位置编码 | 为注意力注入序列顺序信息（注意力本身无序） | 绝对位置编码 / 相对位置编码 / RoPE 旋转位置编码 |
| 模型结构 | 主流为 Decoder-only（GPT 系），另有 Encoder-only（BERT）、Encoder-Decoder（T5） | 生成任务主流是 Decoder-only + Causal Mask |
| 预训练 | 在海量无标注语料上做自监督学习（Next Token Prediction） | 获得通用语言能力；成本最高、周期最长 |
| 后训练 | 在预训练基座上对齐人类意图：SFT、RLHF、DPO、RLVR 等 | 决定模型「好不好用、安不安全」 |
| 数据蒸馏 | 用强模型（教师）生成／筛选数据，训练弱模型（学生） | **【修正】** 此处语境是「蒸馏数据」而非仅模型蒸馏：成本低、见效快，是后训练数据的重要来源 |
| 大模型推理 | 输入 Prompt，自回归逐 Token 生成输出的过程 | 分 Prefill（首 Token）和 Decode（逐 Token）两阶段 |
| 推理 Scaling Law | 推理阶段增加计算量（更长 CoT、多次采样）可提升效果 | 即 Test-Time Compute Scaling，o1/R1 类推理模型的理论基础 |
| 智能体（Agent） | 能感知环境、自主规划、调用工具、完成目标的 LLM 应用形态 | 详见模块三 |

### 1.2 关键问题解答

**`Q1：人工智能、传统 NLP、大模型、大语言模型的关系？`**

> `包含关系：AI ⊃ NLP ⊃ 大模型（在 NLP 领域的形态）⊃ LLM。传统 NLP 是「一个任务一个模型」，靠人工特征和标注数据；LLM 用统一架构 + 海量无标注语料 + 自监督训练，一个模型通过 Prompt 适配多种任务。对测试的影响：传统 NLP 评测是「单任务固定指标」，LLM 评测变成「开放式生成、需要裁判模型和体系化基准」。`

**`Q2：LLM 的运作原理？`**

> 三步：① 分词与嵌入——文本切成 Token 并映射为向量；② Transformer 前向计算——多层自注意力 + 前馈网络，建模上下文依赖；③ 自回归解码——基于概率分布逐个预测下一个 Token（采样策略如 Temperature、Top-p 控制随机性），直到生成结束符。本质：**基于上下文的概率分布采样**。

**Q3：LLM 由哪些关键部件组成？**

> 分词器（Tokenizer）→ 嵌入层 → N × Transformer Block（多头自注意力 MHA + 前馈网络 FFN + 残差连接 + 层归一化）→ 输出头（LM Head，映射回词表 logits）→ 采样解码器。面试提一句：注意力负责「信息混合」，FFN 负责「知识存储」。

**Q4：什么是预训练？**

> 在海量无标注语料上以 Next Token Prediction 为目标的自监督训练。作用：习得语言规律、世界知识和初步推理能力，产出基座模型（Base Model）。特点：数据以万亿 Token 计、成本数百万美元级、周期以月计。

**Q5：什么是后训练？有哪些关键技术？**

> 在基座模型上对齐人类意图、激发专项能力。关键技术：
> - **SFT（监督微调）**：用高质量指令-回答对教学，让模型「会对话」；
> - **RLHF（人类反馈强化学习）**：奖励模型 + PPO，让模型「回答得更讨人喜欢、更安全」；
> - **DPO**：跳过奖励模型，直接用偏好对做对比优化，工程更轻；
> - **RLVR（可验证奖励强化学习）**：对数学/代码等有标准答案的任务，用规则验证器给奖励，是推理模型（o1/R1）的关键路线。

**Q6：大模型推理有哪些工程优化手段？**（测开岗高频，结合性能指标记）

> - **KV Cache**：缓存历史 Key/Value，避免重复计算（空间换时间）；
> - **PagedAttention / 连续批处理（Continuous Batching）**：提升 GPU 利用率与吞吐（vLLM 核心）；
> - **量化**：FP16→INT8/INT4，降显存、提速度，代价是精度损失——**需要回归评测兜底**；
> - **投机采样（Speculative Decoding）**：小模型草拟、大模型验证，加速 Decode；
> - **并行与分布式**：张量并行、流水线并行；MoE 路由。
> 测开视角：每项优化都对应可测指标——TTFT（首 Token 时延）、TPOT（逐 Token 时延）、吞吐（QPS/TPS）、显存占用，以及量化前后的**精度回归对比**。

**Q7：训练分哪几个阶段？各自作用？**

> 预训练（学知识与语言）→ 后训练 SFT（学指令遵循与对话格式）→ 偏好对齐 RLHF/DPO（对齐价值观与安全）→ 推理强化 RLVR（强化推理链）。口诀：**「预训练打底、SFT 教说话、偏好对齐管分寸、RL 练推理」**。

**Q8：数据蒸馏的原理？**

> 用强教师模型生成指令数据、思维链或标注，筛选后训练弱学生模型。原理：教师输出本身就是高密度监督信号，学生以小得多的成本逼近教师能力。风险：学生学到教师的偏见与错误模式，且生成数据同质化——**评测时需警惕训练集与评测集同源污染**。

**Q9：为什么需要推理扩展？推理阶段如何扩展？**

> 原因：训练 Scaling Law 边际收益递减，且复杂推理任务单次前向算力不足。推理时扩展手段：更长思维链（CoT）、自我反思与修正、多次采样投票（Self-Consistency / Best-of-N）、搜索式推理（MCTS）。测开视角：推理时扩展让「同一模型不同推理预算表现不同」，**评测必须固定推理预算（max_tokens、采样次数），否则结果不可比**。

**Q10：智能体的基本概念？**

> 见模块三 Q1，一句话：LLM 是大脑，Agent = 大脑 + 感知 + 规划 + 记忆 + 工具执行的行动闭环。

### 1.3 高频追问 / 易错点 / 测试测开视角

- **追问**：Temperature 为 0 输出就完全确定吗？→ 理论上贪心解码确定，但工程上受浮点误差、批处理影响仍可能不稳定，**测评时应固定 seed 并记录推理参数**。
- **易错点**：预训练 ≠ 微调。预训练改的是「知识与能力分布」，微调改的是「行为风格与任务适配」；SFT 学不会全新知识，只能激发与重组。
- **易错点**：Scaling Law 有训练时与推理时两条曲线，别只答一个。
- **测开视角**：模型版本的每次迭代（数据、权重、推理引擎、量化方案）都等价于传统软件的「一次发版」，需要：版本标识 → 基线评测集回归 → 性能压测 → 灰度发布。

---

## 模块二　数据工程与 DCAI

### 2.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
| --- | --- | --- |
| DCAI（数据为中心 AI） | 保持模型架构基本不变，系统性改进数据质量来提升效果的范式 | 对比 MCAI（模型为中心）：**不变的是模型，改进的是数据** |
| 数据工程 | 构建数据采集、传输、存储、加工管道，保障数据可用 | 交付物是数据管道与数据平台 |
| 数据分析 | 基于已有数据做统计、挖掘、可视化，回答业务问题 | 交付物是报表与洞察 |
| 数据科学 | 用统计与机器学习从数据中构建预测/决策模型 | 交付物是模型与算法 |
| 数据管道（Pipeline） | 数据从源头到消费的自动化流转链路 | 可观测、可重放、幂等是工程关键 |
| ETL | 抽取→转换→加载：先清洗加工再入仓 | 适合结构化、目标明确的场景 |
| ELT | 抽取→加载→转换：先进湖/仓再在仓内转换 | 适合大数据、灵活探索，现代数据栈主流 |
| 数据清洗 | 修正或剔除脏数据：去重、缺失值、异常值、格式统一 | 保障数据「正确」 |
| 数据预处理 | 将干净数据加工为模型可用形态：归一化、编码、分词、切分 | 保障数据「可用」 |
| EDA 与可视化 | 探索性数据分析：用统计图表发现分布、异常与规律 | 直方图、箱线图、散点图、热力图 |

> **【修正】** 原资料概念清单编号缺失（7 之后跳 9），缺第 8 项；按知识体系推断应为「数据标注／数据质量」一类概念。[补充]：数据标注——为无标注数据打标签以形成监督信号，质量指标包括标注一致性（IAA/Kappa）。

### 2.2 关键问题解答

**Q1：DCAI 的定义？什么不变？改进什么？**

> DCAI（Data-Centric AI）：以数据为中心迭代 AI 系统的工程范式。**不变的是模型代码与架构，改进的是数据**——覆盖度、标注质量、分布均衡性、噪声比例。吴恩达提出。对测评的意义：评测集本身也是「数据」，DCAI 思想同样适用于评测集建设（持续补漏、去污染、纠错标）。

**Q2：数据质量对 AI 有哪些影响？**

> 垃圾进垃圾出。影响维度：① 准确性——错标直接带偏模型；② 覆盖度——长尾缺失导致边缘场景失效；③ 均衡性——类别倾斜导致多数类偏好；④ 时效性——过期数据导致知识陈旧；⑤ 合规性——隐私与版权问题引发法律风险。测开视角：数据质量缺陷会在评测中表现为「特定场景指标塌陷」，错例分析要能回溯到数据问题。

**Q3：数据工程、数据分析、数据科学的区别？**

> 数据工程修「路」（管道与平台，保障数据流得动）；数据分析看「路况」（统计与报表，回答发生了什么）；数据科学造「车」（建模预测，回答将要发生什么）。面试补一句：测评平台建设中三者角色都出现——数据工程建回流管道，数据分析做错例统计，数据科学训裁判模型。

**Q4：数据采集的主要来源和方法？**

> 公开数据集、网络爬虫、业务日志/埋点、人工标注、众包、合成数据（模型生成 + 规则模板）、数据采购与合作。Agent 时代新增高价值来源：**线上真实会话日志与 badcase 回流**。

**Q5：ETL 与 ELT 的区别？**

> ETL 先转换后加载（数据进仓前已定schema、定口径），可控性强但灵活性差；ELT 先加载后转换（原始数据先入湖/仓，按需加工），灵活、可追溯，是现代云数仓（如基于对象存储 + 计算引擎）的主流。测评数据回流链路通常用 ELT：原始会话先入湖，再按需加工为评测样本。

**Q6：数据清洗与预处理的定位与方法？**

> 清洗解决「对不对」：去重、缺失值（删除/填充）、异常值（3σ/IQR）、格式与编码统一、敏感信息脱敏；预处理解决「能不能用」：归一化/标准化、类别编码、分词、文本切分、数据增强。二者顺序：先清洗后预处理。

**Q7：[补充] 常见数据科学流程？**

> 业界标准 CRISP-DM：业务理解 → 数据理解 → 数据准备 → 建模 → 评估 → 部署，循环迭代。映射到测评：测评目标定义 → 评测集构建 → 执行 → 分析 → 报告 → 回流，本质同构。

**Q8：常见数据分析方法？**

> 描述统计（均值/分位数/分布）、对比分析、分组分析、漏斗分析、留存分析、相关/回归分析、假设检验、A/B 测试。

**Q9：常见数据可视化方法？**

> 分布看直方图/箱线图，趋势看折线图，占比看饼图/堆叠图，关系看散点图，相关性看热力图，地理数据看地图，多维对比看雷达图。

### 2.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：DCAI 不是「不要模型」，而是「迭代的主战场从模型转向数据」。
- **追问**：合成数据能无限用吗？→ 不能，存在分布塌陷与错误自增强，需与真实数据混合并做质量过滤。
- **测开视角**：评测集即数据集，同样适用 DCAI 全流程——采集（线上回流）→ 清洗（去重、脱敏）→ 标注（人工 + 裁判模型预标注）→ 版本管理（可追溯、可 diff）→ 持续扩充（badcase 驱动）。

---

## 模块三　AI Agent 基础与架构

### 3.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
| --- | --- | --- |
| AI Agent | 以 LLM 为大脑，能感知环境、自主规划、调用工具、迭代执行直至完成目标的系统 | 核心公式：Agent = LLM + 规划 + 记忆 + 工具 |
| 环境感知能力 | 通过文本、图像、语音、API 返回等获取环境状态 | 多模态输入是趋势 |
| 工具调用能力 | 按约定格式（Function Calling）调用外部 API/函数 | 评测重点：参数正确性、时机正确性 |
| 记忆能力 | 跨步骤、跨会话保存与检索信息 | 分短期/长期等类型（见 Q8） |
| 视觉语言模型（VLM） | 同时理解图像与文本的多模态模型 | 是 GUI Agent（操作界面）的基础 |
| 思维链（CoT） | 让模型显式输出推理步骤再作答 | 提升推理准确率，也便于轨迹评测 |
| ReAct 模式 | 推理（Thought）与行动（Action/Observation）交替循环 | Agent 执行的经典范式 |
| Reflection 反思 | 对执行结果自我评估、发现错误并修正重试 | 提升成功率的自纠机制 |
| MCP | Anthropic 提出的模型上下文协议，统一 LLM 接入工具/数据源的接口标准 | 类比「AI 应用的 USB-C」 |
| A2A | Google 提出的 Agent-to-Agent 协议，支持异构 Agent 间通信协作 | 解决跨厂商 Agent 互操作 |
| ANP | Agent Network Protocol，面向开放网络的 Agent 身份与通信协议 | 侧重去中心化身份与发现 |
| 开发范式 | 低代码平台编排 vs 代码框架开发 | 见 Q15 |

### 3.2 关键问题解答（16 问）

**Q1：什么是 AI 智能体？**

> 一个能自主完成目标的系统：感知环境输入 → 规划拆解任务 → 调用工具执行 → 观察结果并迭代，直到目标达成或终止条件触发。与纯 LLM 的区别：LLM 只「说」，Agent 能「做」——有行动闭环和环境反馈。

**Q2：AI Agent 的关键特征？**

> 自主性（无需逐步人工指令）、目标导向、规划与推理、工具使用、记忆与上下文、环境交互与反馈迭代。

**Q3：Agent 的典型任务执行流程？**

```text
用户目标 → 任务理解 → 规划拆解（Plan）
   ↓
循环：思考(Thought) → 选择工具/动作(Action) → 执行 → 观察结果(Observation)
   ↓                        ↑__________ 结果不满足则反思(Reflection)修正 ________|
目标达成 / 达到步数上限 → 汇总输出
```

**Q4：怎么理解 Agent 的关键技术能力？**

> 四大支柱：感知（看懂环境，含多模态）、规划（拆解任务，CoT/任务分解）、记忆（短期上下文 + 长期存储检索）、行动（工具调用、代码执行、GUI 操作）。LLM 是调度这四大能力的「大脑」。测开视角：**每项支柱能力都对应独立评测维度**（见模块八）。

**Q5：Agent 常用的感知方式？**

> 文本/结构化 API 返回、图像与截图（VLM）、语音（ASR/TTS）、网页 DOM 与无障碍树（Browser Agent）、代码与文件系统、传感器数据（具身场景）。

**Q6：触发模型推理的三种常见模式？**

> ① 直接生成：单轮 Prompt → 直接出答案；② 思维链（CoT）：先显式推理再给结论；③ ReAct：推理与工具调用交替的多轮循环。复杂度与算力消耗递增，适用任务难度也递增。

**Q7：ReAct 模式的交互流程？**

```text
Question: 用户问题
Thought 1: 我需要先查 X      → Action 1: search("X")
Observation 1: 搜索结果
Thought 2: 还需确认 Y        → Action 2: query_api("Y")
Observation 2: 接口返回
Thought 3: 信息已足够        → Action 3: finish("最终答案")
```

> 面试加分：ReAct 把推理轨迹暴露出来，**天然具备可观测性**，是 Agent 轨迹评测的基础。

**Q8：常见的四种记忆类型？**

> ① 短期/工作记忆：当前会话上下文窗口内的信息；② 长期记忆：向量库/外部存储，跨会话持久，靠 RAG 检索召回；③ 情景记忆：历史任务的经历与结果（案例库），用于经验复用；④ 语义记忆：抽象出的知识与事实（知识库/知识图谱）。类比人类心理学：工作记忆、情景记忆、语义记忆、程序性记忆（技能/流程）。

**Q9：为什么需要 MCP 协议？**

> 没有协议前，每个工具都要为每个模型/框架定制接入（M×N 问题）。MCP 定义统一的 Client/Server 标准：工具方实现一次 MCP Server，所有支持 MCP 的 Agent 都能用，M×N 降为 M+N。工程价值：解耦、复用、生态标准化；测试价值：**工具接口有了标准契约，可以做契约测试与 Mock**。

**Q10：为什么需要 Multi-Agent？**

> 单 Agent 的局限：上下文窗口有限、角色混杂导致提示词臃肿、复杂任务规划深度不足、单点失败。Multi-Agent 通过分工（专业化角色）、隔离（独立上下文）、协作（并行/流水线）提升复杂任务的完成质量与可维护性。

**Q11：如何使用 Multi-Agent？**

> 步骤：任务分析 → 角色拆分（如规划者/执行者/检查者）→ 定义各 Agent 的职责与工具集 → 设计通信协议与消息格式 → 选择协作架构 → 编排调度与异常处理 → 端到端验收。原则：能单 Agent 解决就不上多 Agent——协作本身引入通信成本与新的失败模式。

**Q12：Multi-Agent 通过什么协议协作？**

> 应用层协议：MCP（Agent↔工具）、A2A（Agent↔Agent，任务委托与消息）、ANP（开放网络中的身份与发现）。工程实践中也常用框架内部消息总线（如 AutoGen 的消息机制）。

**Q13：Multi-Agent 协作的三种架构？**

> ① 中心化（主从/Orchestrator）：一个调度者分派与汇总，可控性强；② 去中心化（对等协作）：Agent 间直接通信协商，灵活但难管控；③ 分层/混合式：高层规划 + 低层执行分组，兼顾可控与扩展。

**Q14：如何从零构建一个 Agent？**

```text
1. 定义目标与能力边界（做什么、不做什么）
2. 选基座模型（能力、成本、时延权衡）
3. 设计系统提示词（角色、SOP、约束）
4. 接入工具（Function Calling / MCP Server）
5. 实现记忆（上下文管理 + 长期记忆检索）
6. 搭建执行循环（ReAct Loop：思考→行动→观察）
7. 加护栏（权限、审批、超时、重试、兜底）
8. 建评测集并迭代（badcase 驱动优化）
```

**Q15：AI Agent 的两种开发范式？**

> ① 低代码/平台编排范式：Dify、Coze 等，拖拉拽工作流，快、门槛低，适合业务验证；② 代码框架范式：LangChain/LangGraph、AutoGen、自研 Harness，灵活可控、可深度定制，适合生产级复杂系统。选型看定制深度、性能要求与团队工程能力。

**Q16：Agent 落地面临哪些挑战？**（**【修正】** 原资料「AI 建设」表述含混，此处语境应为「AI Agent 建设」）

> ① 可靠性：幻觉与错误级联——一步错步步错，多步任务成功率 = 单步成功率的连乘；② 可评测性：开放式输出难断言，缺少标准答案；③ 成本与时延：多轮推理 token 消耗大；④ 上下文限制：长任务超出窗口；⑤ 安全：提示注入、越权工具调用、数据泄露；⑥ 工程复杂度：状态管理、异常恢复、可观测性要求高。**这六点每一点都是测开岗的切入面。**

### 3.3 高频追问 / 易错点 / 测试测开视角

- **追问**：Agent 和 Workflow 的区别？→ Workflow 路径由人预先编排，Agent 路径由模型动态决策；生产中常用「受控 Agent」= 骨架 Workflow + 局部自主决策。
- **易错点**：MCP 解决 Agent↔工具，A2A 解决 Agent↔Agent，不要混。
- **测开视角**：Agent 的失败具有**轨迹性**——不能只看最终答案，要回放完整执行轨迹定位失败步骤；工具接口要 Mock 化以支持离线确定性回归。

---

## 模块四　Prompt Engineering

### 4.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
| --- | --- | --- |
| 提示词工程 | 通过设计与优化输入文本，引导模型产出期望输出的方法 | 不改权重、成本最低的能力激发手段 |
| 专业 Prompt 四要素 | 角色、目标、要求/约束、上下文/示例 | 口诀：立角色、定目标、补要求、给上下文 |
| 动态示例选取 | 按当前输入语义相似度，动态检索最相关的 few-shot 示例 | 比固定示例效果好；本质是一次小型 RAG |
| 自动提示工程（APE） | 用算法/模型自动搜索、生成、评估提示词 | 提示词从「手艺」走向「可优化的工程对象」 |
| 多模态信息提取 | 从图文混合内容中结构化抽取指定字段 | 强调 schema 约束与示例 |
| 多模态检索 | 跨模态（文搜图、图搜文）的检索任务 | 依赖多模态向量空间对齐 |
| 生成式 Prompt | 以开放生成为目标的提示词（写作、对话等） | 评价主观，常需裁判模型/人工 |
| 检索式 Prompt | 以信息查找与准确回答为目标的提示词 | 强调事实性与引用，常与 RAG 结合 |

### 4.2 关键问题解答（10 问）

**Q1：什么是 Prompt？**

> 给模型的输入指令，是人与模型交互的接口，包含任务描述、上下文、示例、约束等。Prompt 不修改模型参数，只通过上下文引导模型激活相应能力——本质上是「用自然语言编程」。

**Q2：Prompt 的典型应用场景？**

> 文本生成（写作、摘要、翻译）、信息抽取（结构化字段提取）、分类与标注、代码生成、问答对话、数据构造（合成评测集/训练集）、作为裁判模型评分。**测开岗尤其要记住后两个：Prompt 是构造评测数据和自动评分的核心工具。**

**Q3：写好 Prompt 的四要素？**

> ① 角色（Role）：你是谁，决定语气与专业视角；② 目标（Task）：要做什么，动词开头、单一明确；③ 要求/约束（Constraints）：格式、长度、风格、禁止项、输出 schema；④ 上下文/示例（Context/Few-shot）：背景信息与输入输出样例。

**Q4：优化 Prompt 的几个方向？**

> ① 迭代调优：基于 badcase 逐步修订；② 任务拆分：复杂任务拆成多步/多 Prompt 链；③ 增加示例：Few-shot 校准格式与风格；④ 结构化：用分隔符、标题、模板明确区块；⑤ 引导推理：CoT、先思考后作答；⑥ 自动化：APE 自动搜索优化。配合评测集做 A/B 对比，用数据说话而非手感。

**Q5：开发中如何立角色、定目标、补要求？**

> 立角色：给专业身份 + 能力描述（"你是资深测试开发工程师"）；定目标：一句话说清任务 + 明确产出物；补要求：输出格式（JSON schema）、边界（不知道就说不知道）、质量标准、异常处理。三者缺一，输出方差就会显著增大。

**Q6：常见 Prompt 分隔符？**

> 三引号 `"""`、三个反引号 ```` ``` ````、XML 标签 `<context>...</context>`、横线 `---`、井号标题 `###`。作用：区分指令与数据，**防提示注入的第一道工程手段**。

**Q7：图像生成的 Prompt 三要素？**

> 主体（画什么）、风格（什么画风/质感）、细节修饰（构图、光线、色彩、镜头参数）。越具体越可控。

**Q8：多模态信息提取提示词的三大核心技巧？**

> ① 明确输出 schema：给出 JSON 模板与字段定义，缺失字段填 null；② 给图文对照示例：few-shot 展示从图到结构化结果的映射；③ 约束提取范围：只提取图中可见信息、禁止臆测，降低幻觉。

**Q9：为什么多模态提示词重要？**

> 真实业务输入是多模态的（截图、单据、图表）；提示词要把「看图任务」转化为明确的视觉指令——告诉模型看什么区域、提什么信息、按什么格式输出，否则 VLM 容易泛泛描述而非精确提取。GUI Agent、票据识别、文档理解都依赖此能力。

**Q10：图文混合检索提示词设计的核心原则？**

> ① 查询意图显式化：把模糊的混合输入改写为清晰检索意图；② 模态角色分工：文本给语义约束、图像给视觉约束，提示词说明以谁为主；③ 输出可排序：要求返回相关性依据，便于重排与评测。

### 4.3 高频追问 / 易错点 / 测试测开视角

- **追问**：Prompt 调优怎么证明有效？→ 建评测集 + 自动化评分 + A/B 对比，拒绝「我觉得变好了」。
- **易错点**：Few-shot 示例会显著影响输出分布，**变更示例 = 变更被测对象**，要纳入版本管理。
- **测开视角**：Prompt 是「代码资产」——要版本化、要评审、要回归测试；提示注入测试是安全专项（越权指令、分隔符逃逸、间接注入）。

---

## 模块五　Context Engineering

### 5.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
| --- | --- | --- |
| Context Engineering | 系统性管理进入模型上下文窗口的全部信息，让模型在正确时间看到正确信息 | Prompt 管「怎么说」，CE 管「给什么看」 |
| 上下文压缩 | 将冗长历史信息浓缩为更短的等价表示 | 应对窗口上限的核心手段 |
| 记忆和摘要 | 把历史交互摘要化存入记忆，需要时召回 | 摘要是有损压缩，需保关键信息 |
| 压缩时机 | 触发压缩的条件：接近窗口阈值、任务阶段切换、话题切换 | 压早了丢信息，压晚了超窗口 |
| 过滤 | 按相关性筛除无关/低价值上下文 | 降噪，提升信噪比 |
| Agentic Context Engineering | 由 Agent 自主决策上下文的写入、选取、压缩、隔离 | CE 的高级形态：上下文成为被主动管理的资源 |

### 5.2 关键问题解答

**Q1：什么是 Context Engineering？**

> 围绕上下文窗口的系统工程：决定每一步推理时，窗口里放哪些信息、以什么形式、放多少。背景：上下文窗口是稀缺资源，塞少了模型「无知」，塞多了成本飙升且「大海捞针」效应导致关键信息被忽略（Lost in the Middle）。CE 的目标是在预算内最大化有效信息密度。

**Q2：CE 的实现方法及作用？**（四类，记忆口诀：**写、选、压、隔**）

> ① 写入（Write）：把重要信息持久化到外部记忆/草稿板，窗口外保存；② 选取（Select）：按需检索最相关的记忆、工具说明、示例进入窗口；③ 压缩（Compress）：摘要、截断、提炼历史；④ 隔离（Isolate）：拆分子任务给 Sub-Agent，各自独立上下文，只回传结论。

**Q3：压缩的注意事项？什么时候开始压缩？**

> 注意事项：保留关键实体与结论（任务目标、已确认事实、未完成事项）、保留可追溯指针（原文出处/记忆 ID）、避免压缩引入的摘要幻觉、压缩后做回归验证。触发时机：上下文占用接近阈值（如 70–80%）、任务阶段切换点、检测到话题漂移时。[补充] 具体阈值各系统不同，属工程调参范畴。

**Q4：Sub-Agent 的 Context 是相互隔离的吗？**

> 通常是隔离的：每个 Sub-Agent 有独立上下文窗口，主 Agent 通过结构化消息派发任务、接收结论。隔离的价值：节省主上下文、角色专注、失败局部化；代价：信息共享靠显式传递，可能丢上下文细节——设计时要定义好消息协议（输入契约 + 输出契约）。

### 5.3 高频追问 / 易错点 / 测试测开视角

- **追问**：长上下文模型（1M token）出来了，CE 还需要吗？→ 需要。窗口大 ≠ 利用好，长上下文的召回率仍衰减，且成本/时延随长度增长，CE 从「救急」变为「优化信噪比与成本」。
- **测开视角**：压缩/摘要策略变更必须做**长对话回归评测**；构造专项用例验证「关键信息跨压缩保留」；隔离架构下要测消息契约的兼容性。

---

## 模块六　RAG、微调与知识增强

### 6.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
| --- | --- | --- |
| RAG | 检索增强生成：先检索相关知识，再连同问题一起交给模型生成答案 | 给模型「开卷考试」 |
| 微调 | 用领域数据继续训练模型，调整权重以适配任务/风格 | 改的是模型「能力与行为」 |
| 提示工程 | 不改权重，通过输入设计激发模型已有能力 | 三者中成本最低、见效最快 |
| 语义分片 | 按语义完整性（而非固定长度）切分文档 | 保证每个 chunk 语义自洽 |
| 向量化 | 用 Embedding 模型把文本映射为稠密向量 | 语义检索的数学基础 |
| 检索 | 根据查询从知识库中找出相关文档片段 | 分稀疏（关键词）与稠密（向量） |
| 混合检索 | 稀疏检索（BM25）+ 稠密检索（向量）融合，取长补短 | 再经重排（Rerank）精排 |
| 增强生成 | **【修正】** 原文「增强式」表述不完整，应为 RAG 中的「增强（Augmented Generation）」环节：把检索结果注入 Prompt 上下文供生成使用 | 连接检索与生成的桥梁 |

### 6.2 关键问题解答（10 问）

**Q1：RAG 的定义？**

> Retrieval-Augmented Generation：在生成前先从外部知识库检索与问题相关的内容，将其作为上下文注入 Prompt，让模型基于检索证据生成答案。价值：知识可更新（改库不改模型）、可溯源（引用出处）、抑制幻觉、成本低。

**Q2：大模型应用落地的四种常见技术选型？**

> ① Prompt 工程：零成本试错，适合通用任务快速验证；② RAG：知识频繁更新、需溯源的场景；③ 微调（SFT 等）：固化领域风格/格式/专项能力；④ 预训练/继续预训练：领域知识与通用语料差异极大时（如专业垂直领域）。复杂度与成本递增，实践中常组合使用（Prompt + RAG 打底，必要时微调）。

**Q3：选 RAG 还是微调，考虑哪五个关键点？**

> ① 知识更新频率：常变选 RAG；② 需求类型：补知识选 RAG，改行为/风格选微调；③ 数据条件：微调需足量高质量标注，RAG 只需文档库；④ 可解释性：RAG 可引用溯源，微调是黑盒；⑤ 成本与周期：RAG 上线快、迭代快，微调训练与回归成本高。一句话：**「知识靠检索，能力靠微调」**。

**Q4：RAG 工作流程的关键步骤？**

```text
【离线索引】文档解析 → 语义分片 → 向量化(Embedding) → 写入向量库
【在线问答】用户问题 → 查询改写 → 混合检索(BM25+向量) → 重排(Rerank)
           → 上下文组装(增强) → LLM 生成 → 答案 + 引用
```

**Q5：文档解析的定义和作用？**（**【修正】** 原文「文档解析 MRC」中 MRC 通常为 Machine Reading Comprehension（机器阅读理解）的缩写，与文档解析并列属术语混用；按上下文此处应指文档解析（Document Parsing）环节，MRC 标注存疑 [补充]）

> 文档解析：将 PDF/Word/扫描件等富格式文档提取为结构化纯文本（含版面、表格、标题层级）。作用：RAG 的第一公里——解析质量直接决定分片与检索质量，表格错乱、页眉页脚污染是常见的低质答案根因。测开视角：解析环节要有独立的准确率评测（如表格结构还原率）。

**Q6：什么是语义分片？为什么需要？有哪些方案？**

> 按语义边界切分文档，保证每片语义完整独立。为什么：固定长度切分会切断语义（半句话、断表格），导致检索召回的是残缺的上下文。方案：① 按结构切（标题、段落、章节）；② 递归切分（先大段后补刀，控制长度）；③ 语义切分（Embedding 相似度突变点切分）；④ 滑动窗口重叠切（缓解边界截断）。

**Q7：什么叫向量化？优势？**

> 用 Embedding 模型将文本编码为固定维度稠密向量，语义相近则向量距离近。优势：突破关键词字面匹配（"汽车"能命中"轿车"）、支持跨语言、计算高效（近似最近邻 ANN 索引毫秒级检索）。

**Q8：为什么需要混合检索？**

> 向量检索语义强但对精确词（编号、专有名词、缩写）不敏感；BM25 关键词精确匹配但无语义理解。二者融合（如 RRF 倒数排名融合）互补短板，再经 Rerank 模型精排，召回率与精确率兼得。

**Q9：什么是大模型幻觉？**

> 模型生成看似合理但与事实不符或无依据的内容。成因：训练数据噪声、知识边界不清、生成本质是概率采样。治理手段：RAG 提供证据、提示词约束（"不知道就说不知道"）、引用溯源、**评测中加入事实性/忠实度指标（答案是否被检索内容支撑）**。

**Q10：RAG 增强环节完成什么功能？**

> 把检索重排后的 Top-K 片段按模板组装进 Prompt（控制长度、去重、标注来源），为生成提供上下文证据，并可在 Prompt 中约束「仅依据所给资料作答 + 标注引用」。它决定了检索到的知识能否被模型真正用上——**「检索得到」不等于「用得上」**，增强与生成环节需要联合评测（如 Faithfulness 忠实度）。

### 6.3 高频追问 / 易错点 / 测试测开视角

- **追问**：RAG 效果不好怎么排查？→ 分层定位：检索层（召回率：该查到的查到了吗）→ 增强层（上下文组装是否合理）→ 生成层（忠实度：是否依据证据作答）。各层独立建指标，避免一锅端。
- **易错点**：Embedding 模型升级会导致全库向量失效，需要重建索引——属于重大版本变更，要全量回归。
- **测开视角**：RAG 评测指标三件套——检索召回率/命中率、忠实度（Faithfulness）、答案相关性（Answer Relevancy）；构建带标准证据标注的评测集是关键资产。

---

## 模块七　Harness Engineering

### 7.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
| --- | --- | --- |
| Harness Engineering | 设计与构建包裹模型的运行时「脚手架」：执行循环、工具接入、状态管理、权限护栏、工作流 | 模型是引擎，Harness 是整车底盘与控制系统 |
| 认知框架 | 通过系统提示、角色设定、思维模板塑造模型「如何思考问题」 | 决定模型的行为模式与推理风格 |
| 能力边界 | 明确模型能做什么、不能做什么：工具白名单、权限、资源配额 | 安全与可控性的核心 |
| 模型行为 | 模型在系统中的实际输出与动作序列 | 用标准工作流程（SOP）规约行为 |

### 7.2 关键问题解答（4 问）

**Q1：Prompt、Context、Harness 三者的差异？**（本模块核心问题）

| 维度 | Prompt Engineering | Context Engineering | Harness Engineering |
| --- | --- | --- | --- |
| 管理对象 | 单条指令的措辞与结构 | 进入窗口的全部信息组合 | 模型外的整个运行时系统 |
| 作用范围 | 一次调用 | 一次会话/任务的上下文流 | 系统全生命周期 |
| 关注点 | 怎么说 | 给什么看 | 怎么跑、怎么管、怎么兜底 |
| 类比 | 一句话术 | 会议纪要+资料袋 | 整套生产线与操作规程 |

> 面试话术：三者层层递进——Prompt 优化单点，Context 优化信息流，Harness 把模型变成可靠系统组件。**测试对象的层级也对应上升：测输出 → 测上下文行为 → 测系统级任务完成。**

**Q2：如何控制认知框架？**

> 通过系统提示词固化角色与立场、提供思维模板（如 ReAct 的思考格式、检查清单式推理）、注入领域知识与原则、用 few-shot 示范期望的推理路径。本质：在每次推理前给模型戴上「统一的思维框架」，减少行为漂移。

**Q3：如何控制能力边界？**

> 工具层面：工具白名单、参数校验、最小权限；权限层面：敏感操作人工审批（Human-in-the-loop）、分级授权；资源层面：步数上限、token 预算、超时与熔断；环境层面：沙箱执行、网络隔离。原则：**默认拒绝，显式授权**。

**Q4：如何用标准工作流程控制模型行为？**

> 把最佳实践固化为 SOP：定义任务的标准步骤序列与每步的检查点，用状态机约束流转（哪些状态允许哪些动作），关键节点设验证与审批，异常路径预定义兜底策略。效果：模型在框架内自主，行为可预期、可审计、可回归。测开视角：**SOP 即测试用例的天然骨架——每个检查点就是一个断言点。**

### 7.3 高频追问 / 易错点 / 测试测开视角

- **追问**：Harness 和 Agent 框架（LangGraph 等）的关系？→ 框架是实现 Harness 的工具，Harness 是设计思想（执行循环 + 状态 + 护栏 + 观测）。
- **测开视角**：Harness 是 Agent 系统「可测性」的主要来源——执行轨迹日志、状态快照、工具调用记录、检查点断言，全部由 Harness 产出；面试中可强调「可观测性设计优先于评测设计」。

---

## 模块八　Agent 评测 vs LLM 评测

### 8.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
| --- | --- | --- |
| NL2API 评测 | 自然语言 → API/函数调用：评测模型选择正确工具、生成正确参数的能力 | 指标：工具选择准确率、参数正确率、调用成功率 |
| NL2SQL 评测 | 自然语言 → SQL：评测语义理解与 schema 对齐能力 | 指标：执行准确率（EX）＞ 精确匹配（EM） |
| NL2Code 评测 | 自然语言 → 代码：评测代码生成能力 | 指标：pass@k（单元测试通过率），基准如 HumanEval |
| 记忆能力评测 | 评测 Agent 跨轮次/跨会话的信息存取正确性 | 长上下文检索、记忆一致性、遗忘曲线 |
| 规划能力评测 | 评测任务拆解与多步推理的合理性与有效性 | 分 CoT 轨迹评测与 ReAct 轨迹评测 |
| Multi-Agent 协同评测 | 评测多 Agent 分工、通信、汇总的系统级表现 | 通信开销、死锁、涌现行为 |
| 长期记忆 RAG 评测 | 评测长期记忆的写入、检索、更新质量 | 本质是按 RAG 指标体系测记忆子系统 |

### 8.2 关键问题解答（7 问）

**Q1：Agent 测评与 LLM 测评有什么区别？**（必考题）

| 维度 | LLM 测评 | Agent 测评 |
| --- | --- | --- |
| 被测对象 | 模型本身（单点能力） | 模型+工具+记忆+Harness 的完整系统 |
| 评测粒度 | 单轮输入输出 | 多步执行轨迹 + 最终结果 |
| 判定标准 | 答案质量（对错/好坏） | 任务完成（成功率）+ 过程质量（轨迹合理性） |
| 环境依赖 | 无状态、静态 | 有状态、依赖真实/仿真环境 |
| 失败模式 | 答错、幻觉 | 错误级联：一步错导致全盘失败 |
| 核心指标 | 正确率、ROUGE/BLEU、质量分 | 任务成功率、步数效率、token 成本、工具调用正确率 |
| 评测方法 | 静态评测集 + 裁判模型 | 动态环境 + 轨迹回放 + 过程断言 |

> 一句话：**LLM 测评测「说得好不好」，Agent 测评测「事办成没有、怎么办成的」。**

**Q2：NL2API / NL2SQL / NL2Code 的原理？**

> 三者都是「自然语言 → 形式化语言」的语义解析任务：
> - **NL2API**：将用户意图映射为工具/函数调用（名称 + 结构化参数），原理是 Function Calling 的模式匹配与参数填充；评测看选对了没有、参数对了没有、调成了没有。
> - **NL2SQL**：理解自然语言问题并对齐数据库 schema 生成 SQL；评测金标准是看**执行结果**是否与标准 SQL 一致（执行准确率），而非字面对齐。
> - **NL2Code**：由需求描述生成代码；评测靠**跑测试用例**（pass@k：k 次采样中至少一次通过全部单测的概率）。
> 共同点：**可执行、可自动判定**——是 Agent 评测中自动化程度最高的部分。

**Q3：记忆的存储方法、操作技术有哪些？**

> 存储方法：向量数据库（语义检索）、KV 存储（精确存取）、关系库/图数据库（结构化事实与关系）、文件系统（日志与文档）。操作技术：写入（去重、冲突检测）、检索（相似度 + 时间衰减 + 重要性加权）、更新（合并、纠正旧记忆）、遗忘（衰减策略、容量淘汰）、整合（摘要提炼成长期知识）。

**Q4：长期记忆为什么需要 RAG？**

> 长期记忆数据量大且持续增长，不可能全塞进上下文窗口；RAG 提供按需检索机制——当前任务需要什么记忆就召回什么。长期记忆 = 存储层，RAG = 检索与注入层，二者配合实现「无限记忆、有限窗口」。评测上：记忆召回不全或召回错误会直接表现为任务失败，因此需要记忆系统的独立评测。

**Q5：单 Agent 规划 CoT 如何测评？**

> CoT 是静态推理链，测评方法：① 结果评测——最终答案正确率；② 轨迹评测——用裁判模型评估推理步骤的逻辑性、完整性、有无跳步/循环；③ 一致性评测——多次采样答案一致性（Self-Consistency）；④ 对比评测——与参考推理链的相似度。注意：**CoT 轨迹长≠质量好**，要防止「表演式推理」。

**Q6：单 Agent 规划 ReAct 如何测评？**

> ReAct 是「思考-行动-观察」的动态轨迹，测评方法：① 终局指标——任务成功率；② 轨迹对齐——工具调用序列与专家参考轨迹的匹配度；③ 过程断言——每一步工具选择、参数、时机是否正确；④ 效率指标——步数、token 消耗、无效/重复调用率；⑤ 恢复能力——注入工具故障，观察纠错与重试行为。工程手段：**工具 Mock 化 + 轨迹录制回放**，保证可重复、可断言。

**Q7：Multi-Agent 系统测评方法有何不同？**

> 在单 Agent 评测基础上增加系统级维度：① 通信开销（消息轮数、token 放大倍数）；② 分工合理性（角色负载、能力匹配）；③ 协同质量（信息传递失真度、结论汇总正确性）；④ 故障模式（死锁、循环对话、责任推诿）；⑤ 涌现与收敛（是否收敛到一致结论、有无群体幻觉）。方法上需端到端任务成功率 + 全链路消息日志审计。

### 8.3 高频追问 / 易错点 / 测试测开视角

- **追问**：Agent 成功率低怎么归因？→ 建失败分类法：规划错/工具错/记忆错/环境错/模型错，按轨迹回放归类统计，帕累托分析找主因。
- **易错点**：Agent 评测不能复用静态评测集思路——环境状态、工具返回都会影响结果，必须固化环境快照（Mock/仿真）才能回归。
- **测开视角**：非确定性是最大敌人。对策：固定温度与 seed、Mock 外部依赖、录制回放、多次采样取分布（pass^k / 成功率均值±方差）而非单次定论。

---

## 模块九　LLM 大模型测评体系（本岗核心，务必吃透）

### 9.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
| --- | --- | --- |
| 策略评估价值 | 测评支撑的六类决策：发布决策、底座选型、创新改进、学术研究、第三方测评、行业准入 | 测评是「决策依据」而非「技术表演」 |
| 被测对象三层结构 | ① 模型本身；② 作为代理的应用（Agent 应用层）；③ 全栈 AI 产品（端到端用户体验） | 层级越高越接近业务，越需要系统化评测 |
| 测评能力框架 | 三大支柱：测评基线数据自动生成、专项测试技术、工程工具与流程规范 | 数据、技术、流程三位一体 |
| 测评流程活动 | 测试策略设计 → 测试数据集构建 → 测试执行 → 测试结果分析 → 测试评估报告 | 与传统测试流程同构，内容不同 |
| 数据集核心抓手 | 覆盖全、数量多、测评快 | 评测集是测评体系的第一资产 |
| 功能性质量属性 | 准确性、相关性、可用性、信息量、一致性、泛化性、逻辑性 | 「答得好不好」的七维刻画 |
| 可靠性质量属性 | 稳定性、鲁棒性、校准与不确定性 | 「答得稳不稳」的三维刻画 |
| 安全合规 | 隐私保护、安全、防攻击、毒性、无害内容、合规、公平性 | 红线属性，一票否决 |
| 性能效率 | 并发、首 Token 时延（TTFT），涵盖训练性能与推理性能 | 线上体验与成本的直接指标 |
| STA 测试设计 | 场景-任务-能力（Scenario-Task-Ability）三层拆解，构建测评基线大纲的方法论 | 从业务场景反推评测集结构 |
| 数据回流 | 线上 badcase 与业务数据回流至评测集与训练，形成数据飞轮 | 测评从「一次性活动」变为「持续循环」 |
| 裁判模型 | 用强模型基于规则与统计方法自动判定被测模型输出 | LLM-as-a-Judge，降本提效 |
| 绝对测评 | 与参考答案对比：正确率、ROUGE、BLEU 等 | 有标准答案时用 |
| 相对测评 | 多模型对比打分/排序：质量分数、对战胜率、Elo 评级 | 开放生成、无标准答案时用 |

### 9.2 关键问题解答（15 问）

**Q1：什么是 LLM 模型测评？**

> 系统性地构造输入、执行模型、度量输出质量，为模型研发与应用的决策提供量化依据的工程活动。与传统软件测试的本质差异：被测对象具有不确定性（同输入不同输出）、输出是开放式的（无唯一标准答案）、能力边界是概率性的——因此测评方法论从「断言对错」演进为「统计度量 + 裁判模型 + 体系化基准」。

**Q2：大模型测评能力框架包含哪几部分？**

> 三大支柱：① 测评基线数据自动生成——评测集是核心抓手，要求覆盖全、数量多、测评快，靠自动生成突破人工构造瓶颈；② 专项测试技术——针对各质量属性的专项方法（鲁棒性扰动、安全攻击、性能压测等）；③ 工程工具与流程规范——测评平台、流水线、报告规范，保证测评可重复、可追溯。

**Q3：大模型系统的被测对象有哪些层级？**

> 三层：① 模型本身——基础能力与质量属性（对应模型选型与发版决策）；② 作为代理的应用——Agent/RAG 等系统级能力（工具调用、规划、记忆）；③ 全栈 AI 产品——端到端用户体验与业务指标。层级越高，环境依赖越重、评测成本越高、但越接近真实价值。测试策略上三层互补：底层高频回归，上层抽检验收。

**Q4：核心质量属性包括哪些方面？**

> 三组：① 功能性——准确性、相关性、可用性、信息量、一致性、泛化性、逻辑性；② 可靠性——稳定性、鲁棒性、校准与不确定性；③ 安全合规——隐私保护、防攻击、毒性、公平性、合规等；外加贯穿性的性能效率（TTFT、并发、吞吐）。面试建议按「功能-可靠-安全-性能」四象限背诵。

**Q5：测评专项技术包括哪些方面？**

> [补充] 基于质量属性展开：鲁棒性测试（输入扰动、对抗样本）、安全测试（提示注入、越狱攻击、隐私泄露探测）、性能压测（并发梯度、时延分布）、幻觉检测（事实核查、忠实度判定）、公平性测试（群体偏见探测）、长上下文测试（大海捞针 NIAH）等。

**Q6：业界主要测评基准有哪些？**

> 通用知识推理：MMLU、C-Eval/CMMLU（中文）、AGIEval、GSM8K（数学）；代码：HumanEval、MBPP、SWE-bench；指令遵循与对话：MT-Bench、AlpacaEval、IFEval；综合体系：HELM、SuperCLUE（中文）、OpenCompass；Agent 方向：AgentBench、GAIA、SWE-bench。面试加一句：**基准分数要警惕过拟合与数据污染——公开基准可能已进入训练语料，因此自建私有评测集是刚需。**

**Q7：什么是不确定性与校准？什么是鲁棒性？**

> 不确定性：模型对自身输出正确性的把握程度；校准：模型表达的置信度与实际正确率的匹配程度（说 90% 把握就真有约 90% 正确）。校准差的模型「自信地胡说」最危险。鲁棒性：输入发生扰动（错别字、改写、噪声、格式变化）时输出保持稳定的能力——同义改写后答案不应翻转。

**Q8：测评基本流程包括哪些活动？**

> 五环：① 测试策略设计——明确目标、对象层级、质量属性权重（用 STA 方法从场景拆解）；② 测试数据集构建——基线数据自动生成 + 人工精修 + 版本管理；③ 测试执行——批量推理、自动化流水线、环境固化；④ 测试结果分析——裁判模型评分 + 错例分析归因；⑤ 测试评估报告——量化结论支撑发布/选型决策。五环之后接数据回流，形成闭环。

**Q9：测试数据集的关键作用？**

> 评测集是测评体系的核心抓手，直接决定测评结论的可信度：覆盖全（场景与能力无盲区，否则漏测）、数量多（统计显著，否则波动大）、测评快（支撑高频回归，否则沦为月报）。评测集本身要版本化、防污染、持续回流扩充。

**Q10：测试数据生成的基本原理？**

> 自动生成三路线：① 模板变异——种子用例 × 参数化模板批量裂变；② 模型生成——用强模型按场景大纲生成题目与参考答案（人机协同审核）；③ 线上回流——真实会话挖掘 badcase 沉淀为回归用例。生成后必须质量门禁：去重、难度分层、答案校验、防污染检查。

**Q11：什么是绝对测评和相对测评？**

> 绝对测评：有参考答案，直接对比度量——正确率、ROUGE、BLEU 等，客观可自动化，但难覆盖开放生成；相对测评：无标准答案时比较多个模型/版本的表现，用裁判模型或人工打质量分、做两两对战（胜率、Elo 评级），适合开放式任务。实践中二者结合：能绝对的用绝对（便宜客观），不能绝对的用相对（贴近体验）。

**Q12：评分规则与错例分析如何进行？**

> 评分规则：为每类任务定义可执行的 rubric（维度 × 等级 × 判据），沉淀为裁判模型的评分模板，规则要版本化并与人工抽检对齐。错例分析：建失败分类体系（知识缺失/推理错误/指令未遵循/格式错误/幻觉…），逐例归因打标，统计分布找主要矛盾，输出改进建议反哺模型与评测集。错例分析是连接「测评」与「改进」的桥梁。

**Q13：裁判模型在结果分析中的作用？**

> 基于规则与统计方法自动分析判定被测模型输出，大幅降低人工成本、支撑规模化高频测评。落地要点：① 用强模型 + 精心设计的评分 rubric；② 定期与人工标注做一致性校验（如 Cohen's Kappa），监控裁判漂移；③ 警惕系统性偏差——位置偏好、长度偏好、自我偏好（自家模型偏向自家输出）；④ 高风险结论（发版红线）保留人工复核。

**Q14：数据回流在测评中的意义？**

> 数据回流构成从业务运营到模型优化的数据飞轮：线上会话 → 挖掘 badcase → 归因分析 → 一部分进评测集（防回归）、一部分进训练数据（促优化）→ 新版本上线再验证。意义：让评测集随真实分布进化、让模型迭代有靶可打，测评从阶段性活动升级为持续运转的闭环。**面试话术：没有数据回流的测评是一次性体检，有数据回流的测评是健康管理。**

**Q15：大模型测评的工程工具主要有哪些？**

> [补充] 分层看：① 评测执行框架——批量推理、多模型并发调度（如 OpenCompass 类开源框架、自研评测平台）；② 裁判与评分——裁判模型服务、rubric 模板引擎；③ 数据集管理——评测集版本化、标注平台；④ 结果分析——指标看板、错例管理系统；⑤ 流水线集成——CI/CD 门禁（模型发版卡评测阈值）；⑥ 观测与回流——线上日志埋点、badcase 挖掘管道。一站式测评平台 + 分层流水线是形态目标。

### 9.3 高频追问 / 易错点 / 测试测开视角

- **追问**：评测集多大算够？→ 没有绝对数字，看统计功效：关键指标置信区间可控、长尾场景覆盖达标；与其堆数量，不如保证场景分布对齐线上真实分布。
- **追问**：裁判模型能完全替代人工吗？→ 不能。定位是「放大器」：承担 90% 规模化初评，人工做抽检校准、争议复核、新维度定义。
- **易错点**：ROUGE/BLEU 只看字面重叠，对 LLM 的同义改写不公平——开放生成任务别拿它们当主指标。
- **测开视角（面试升华段）**：我的工作闭环 = STA 拆解建评测大纲 → 数据自动生成扩评测集 → 流水线执行 + 裁判模型评分 → 错例归因反哺 → 数据回流驱动下一轮。每一环都可自动化、可追溯、可度量。

---

## 第三部分　面试速答清单（可直接背诵）

1. **LLM 本质**：基于 Transformer 的自回归概率模型，靠预测下一个 Token 工作。
2. **训练四阶段**：预训练打底、SFT 教说话、RLHF/DPO 对齐偏好、RLVR 练推理。
3. **DCAI**：模型不变、数据迭代；评测集建设同样适用。
4. **Agent 公式**：LLM（大脑）+ 规划 + 记忆 + 工具 = 有行动闭环的智能体。
5. **ReAct**：Thought → Action → Observation 循环，轨迹天然可观测、可评测。
6. **四种记忆**：短期（上下文）、长期（向量库+RAG）、情景（经验案例）、语义（知识事实）。
7. **协议分工**：MCP 管 Agent↔工具，A2A 管 Agent↔Agent，ANP 管开放网络身份发现。
8. **Prompt 四要素**：立角色、定目标、补要求、给上下文。
9. **Context Engineering 四法**：写入、选取、压缩、隔离。
10. **压缩时机**：接近窗口阈值、任务阶段切换、话题漂移时；保关键实体、留追溯指针。
11. **RAG 一句话**：检索增强生成，开卷考试；知识靠检索、能力靠微调。
12. **RAG 管线**：解析 → 分片 → 向量化 → 混合检索 → 重排 → 增强 → 生成。
13. **混合检索**：BM25 补关键词精确性，向量补语义泛化，Rerank 精排。
14. **Harness vs Prompt vs Context**：Prompt 管怎么说，Context 管给什么看，Harness 管系统怎么跑。
15. **能力边界控制**：工具白名单 + 最小权限 + 人工审批 + 沙箱 + 熔断，默认拒绝。
16. **Agent 评测 vs LLM 评测**：前者测系统任务完成（轨迹+成功率），后者测模型单点输出质量。
17. **NL2X 评测共性**：可执行即可判定——NL2SQL 看执行准确率，NL2Code 看 pass@k，NL2API 看调用正确率。
18. **被测对象三层**：模型本身 → Agent 应用 → 全栈 AI 产品。
19. **质量属性四象限**：功能（准确/相关/可用/信息量/一致/泛化/逻辑）、可靠（稳定/鲁棒/校准）、安全合规、性能效率。
20. **测评五环**：策略设计 → 数据集构建 → 执行 → 结果分析 → 评估报告，外接数据回流闭环。
21. **评测集三要求**：覆盖全、数量多、测评快。
22. **绝对 vs 相对测评**：有答案比指标（正确率/ROUGE/BLEU），无答案比高低（质量分/胜率/Elo）。
23. **裁判模型**：LLM-as-a-Judge 降本提效，但须与人工对齐校准，警惕位置/长度/自我偏好。
24. **数据回流**：线上 badcase → 评测集防回归 + 训练集促优化的数据飞轮。
25. **非确定性对策**：固定采样参数、Mock 外部依赖、轨迹回放、多次采样看分布。

---

## 附录

### 附录 A　术语表

| 术语 | 全称/含义 |
| --- | --- |
| LLM | Large Language Model，大语言模型 |
| Agent | 智能体，LLM+规划+记忆+工具的行动系统 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| MCP | Model Context Protocol，模型上下文协议（Anthropic） |
| A2A | Agent-to-Agent 协议（Google） |
| ANP | Agent Network Protocol，智能体网络协议 |
| ReAct | Reasoning + Acting，推理与行动交替范式 |
| CoT | Chain-of-Thought，思维链 |
| Reflection | 反思模式，自我评估与修正 |
| Harness | 包裹模型的运行时脚手架/控制系统 |
| DCAI | Data-Centric AI，数据为中心的人工智能 |
| SFT | Supervised Fine-Tuning，监督微调 |
| RLHF | Reinforcement Learning from Human Feedback，人类反馈强化学习 |
| DPO | Direct Preference Optimization，直接偏好优化 |
| RLVR | Reinforcement Learning with Verifiable Rewards，可验证奖励强化学习 |
| TTFT | Time To First Token，首 Token 时延 |
| TPOT | Time Per Output Token，逐 Token 时延 |
| STA | Scenario-Task-Ability，场景-任务-能力测评设计法 |
| LLM-as-a-Judge | 裁判模型，用大模型自动评分 |
| EDA | Exploratory Data Analysis，探索性数据分析 |
| ETL/ELT | 抽取-转换-加载 / 抽取-加载-转换 |
| NIAH | Needle In A Haystack，长上下文大海捞针测试 |
| pass@k | 代码生成 k 次采样至少一次通过测试的概率 |

### 附录 B　关键流程代码块

**B1. ReAct 执行循环（伪代码）**

```python
def react_loop(goal, tools, max_steps=10):
    context = build_context(goal)          # 系统提示 + 工具说明 + 记忆
    for step in range(max_steps):
        thought, action = llm.think(context)        # Thought → Action
        if action.type == "finish":
            return action.answer
        observation = tools.execute(action)         # Action → Observation
        context.append(thought, action, observation)
    raise MaxStepsExceeded("触发熔断，转人工或兜底")
```

**B2. RAG 在线问答流程**

```python
def rag_answer(question):
    query = rewrite(question)                        # 查询改写
    cands = hybrid_search(query,                     # 混合检索
                          sparse=bm25(query),
                          dense=vector_search(embed(query)))
    top_k = rerank(query, cands)[:5]                 # 重排精排
    prompt = augment(question, top_k)                # 上下文组装 + 引用约束
    answer = llm.generate(prompt)
    return answer, citations(top_k)                  # 答案 + 溯源
```

**B3. 测评流水线（CI 门禁）**

```bash
# 模型发版评测流水线
1. load_eval_suite(version="v2024.x")      # 加载版本化评测集
2. batch_infer(model=candidate, suite, seed=42, temperature=0)
3. judge_score(outputs, rubric="rubric_v3") # 裁判模型评分
4. regression_diff(vs=baseline_model)       # 与基线对比 diff
5. gate: pass if (准确率 >= 阈值) && (红线用例 100% 通过) && (无指标显著退化)
6. report && archive                        # 评估报告归档，可追溯
```

**B4. 裁判模型评分 Prompt 模板**

```text
你是资深评测专家。请按以下 rubric 对被测答案打分：
【维度】准确性(1-5)、相关性(1-5)、完整性(1-5)
【输入】问题：{question}  参考答案：{reference}  被测答案：{answer}
【要求】先逐项给出判分理由，最后输出 JSON：
{"accuracy": x, "relevance": x, "completeness": x, "total": x, "rationale": "..."}
不知道或无法判断时输出 total=0，禁止臆测。
```

**B5. 数据回流飞轮**

```text
线上会话日志 ──→ badcase 挖掘（聚类/规则/人工标注）
      │                    │
      │              归因分析（失败分类法）
      │               ┌────┴────┐
      │          评测集扩充    训练数据优化
      │               │            │
      └──── 新版本上线验证 ←── 模型迭代发版
```

### 附录 C　指标清单

| 类别 | 指标 | 适用对象 |
| --- | --- | --- |
| 绝对测评 | 正确率、精确匹配 EM、ROUGE、BLEU、F1 | 有标准答案任务 |
| 相对测评 | 质量分、对战胜率、Elo 评级 | 开放生成 |
| 代码能力 | pass@k、执行准确率 EX | NL2Code / NL2SQL |
| 工具调用 | 工具选择准确率、参数正确率、调用成功率 | Agent / NL2API |
| Agent 任务 | 任务成功率、平均步数、token 成本、无效调用率 | Agent 系统 |
| RAG | 召回率/命中率、忠实度 Faithfulness、答案相关性 | RAG 系统 |
| 可靠性 | 扰动一致性（鲁棒性）、多次采样方差（稳定性）、校准误差 ECE | 全层级 |
| 性能 | TTFT、TPOT、吞吐 QPS、并发承载、显存占用 | 推理服务 |
| 裁判质量 | 与人工一致性（Kappa/准确率）、偏差审计 | 裁判模型自身 |
| 评测集健康 | 场景覆盖率、难度分布、去重率、污染检测 | 评测集自身 |

---

## 质量自检确认

1. ✅ 覆盖原资料全部 9 个模块的概念与问题（含编号缺失处的推断标注）；
2. ✅ 每模块含核心概念表、关键问题解答、高频追问/易错点/测开视角三段式；
3. ✅ 全文贯穿可测性、评测指标、评测集、自动化回归、工具链、数据回流等岗位视角；
4. ✅ 错别字与术语问题已标注【修正】（「增强式」→增强生成、「AI 建设」→Agent 建设、「文档解析 MRC」术语存疑、概念编号缺失）；无原始资料之外的事实性编造，补充内容均以 [补充] 或通用工程共识呈现；
5. ✅ 速答清单按「一句话可背诵」标准编写，适合面试前 30 分钟冲刺。