# AI Agent 测试与测开岗面试复习笔记

> 适用岗位：AI Agent 测试开发 / SDET（中高级）
> 说明：本笔记基于你提供的资料重构整理。对原资料中的错别字、编号跳号、术语歧义已标注【修正】；资料未覆盖但面试高频的内容标注【补充】，均不编造具体数据和结论。

---

## 一、面试知识地图

| 层级 | 模块 | 核心问题（举例） | 高频词 / 术语 |
|---|---|---|---|
| L1 基础层 | 大模型基础与训练推理 | LLM 运作原理是什么？预训练与后训练区别？Scaling Law 是什么？ | Transformer、Self-Attention、位置编码、SFT、RLHF/DPO、KV Cache、Scaling Law |
| L1 基础层 | 数据工程与 DCAI | ETL 与 ELT 区别？数据清洗与预处理区别？ | DCAI、数据管道、EDA、数据治理 |
| L2 能力层 | AI Agent 基础与架构 | Agent 的关键能力有哪些？ReAct 交互流程？记忆分几类？MCP/A2A/ANP 是什么？ | 感知-规划-行动、ReAct、Reflection、短期/长期记忆、Multi-Agent |
| L2 能力层 | Prompt Engineering | Prompt 四要素？动态示例选取 vs 静态 Few-shot？ | Role/Task/Constraint/Format、APE、多模态提示 |
| L2 能力层 | Context Engineering | 上下文压缩的时机和方法？Sub-Agent 上下文是否隔离？ | Context Window、摘要、过滤、Lost in the middle |
| L2 能力层 | RAG、微调与知识增强 | RAG 完整流程？RAG vs 微调如何选型？为什么要混合检索？ | Chunking、Embedding、向量检索、Rerank、幻觉 |
| L3 工程层 | Harness Engineering | Prompt / Context / Harness 三者差异？如何控制能力边界？ | 认知框架、能力边界、工作流约束、Guardrail |
| L4 测评层 | Agent 评测 vs LLM 评测 | 两者区别？NL2SQL 怎么评？长期记忆为什么要靠 RAG？ | 端到端任务完成率、规划评测、协同评测 |
| L4 测评层 | LLM 大模型测评体系 | 绝对测评 vs 相对测评？裁判模型的作用？数据回流是什么？ | LLM-as-judge、ROUGE/BLEU、数据飞轮、STA |

**记忆口诀**：基础层解决"模型怎么来的"，能力层解决"模型怎么变成 Agent"，工程层解决"Agent 怎么被稳定地控制"，测评层解决"怎么知道它做得好不好、怎么持续变好"。

---

## 二、模块化笔记

### 模块 1：大模型基础与训练推理

#### 1.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
|---|---|---|
| 人工智能（AI） | 让机器模拟/延伸人类智能（感知、推理、决策、语言）的总称学科 | 层级关系：AI ⊃ 机器学习 ⊃ 深度学习 ⊃ NLP/CV 等子领域 ⊃ 大模型 ⊃ LLM |
| 传统 NLP | 基于规则、统计方法（HMM、CRF）或浅层/中等规模神经网络（RNN、LSTM）针对具体任务（分词、NER、情感分析）单独建模 | 任务孤立、需要大量任务专属标注数据、泛化能力弱，是 LLM 出现前的主流范式 |
| 大模型（Foundation Model） | 参数规模巨大（十亿级以上）、基于海量无标注数据自监督预训练、具备"涌现能力"的通用基座模型，覆盖语言/视觉/多模态 | 关键词：规模效应（Scale）、涌现能力（Emergent Abilities）、通用性（一个模型多任务） |
| 大语言模型（LLM） | 大模型家族中专注自然语言处理的分支，当前主流架构是 Decoder-only Transformer | LLM 是大模型的子集，二者常被混用但概念上 LLM 更聚焦文本/语言 |
| Transformer | 2017 年 *Attention is All You Need* 提出的架构，完全基于自注意力机制并行处理序列，摒弃 RNN 的串行依赖 | Encoder-Decoder 原始结构；BERT 是 Encoder-only，GPT/LLaMA/Qwen 是 Decoder-only（当前生成式 LLM 主流） |
| 注意力机制（Attention） | 让每个 token 在编码时动态"关注"序列中其他相关 token 并加权融合信息 | 公式：Attention(Q,K,V) = softmax(QKᵀ/√d_k)V；Multi-Head Attention 让模型在多个子空间并行捕捉不同类型的依赖关系 |
| 词嵌入（Word Embedding） | 把离散的 token 映射为连续稠密向量，使语义相近的词在向量空间中距离也相近 | One-hot 的缺陷是维度灾难+无语义关联；LLM 中的 embedding 经过多层 Attention 后会变成"上下文相关"的动态表示，区别于 Word2Vec 这类静态词向量 |
| 位置编码（Positional Encoding） | 因为 Self-Attention 本身不感知顺序，需要额外注入 token 的位置信息 | 三类：正弦绝对位置编码（原始 Transformer）、可学习绝对位置编码、相对位置编码；当前主流 LLM（LLaMA/Qwen 等）普遍采用 **RoPE（旋转位置编码）**，外推性更好 |
| 模型结构 | 现代 LLM 主体：Embedding 层 → N 层 Transformer Block（Self-Attention + FFN + 残差连接 + LayerNorm/RMSNorm）→ 输出线性层 + Softmax | 面试常问"为什么 Decoder-only 成为主流"：结构简单统一、天然适配自回归生成任务、扩展性和训练稳定性经验证更好 |
| 大模型训练数据 | 预训练数据：海量互联网文本、代码、书籍、多语言语料（近乎无标注）；后训练数据：人工/合成的高质量指令数据、偏好对比数据 | 数据质量 > 数据数量已成共识；关注数据配比（代码占比、多语言占比、数学占比）、去重、去毒、数据污染（Data Contamination）排查 |
| 大模型预训练（Pre-training） | 在海量无标注语料上做自监督学习（典型任务：Next Token Prediction），让模型习得语言规律、世界知识和基础推理能力 | 计算成本占整个训练流程的绝大部分；决定模型能力的"天花板" |
| 大模型后训练（Post-training） | 预训练之后对模型做对齐和能力激发，主要包括 **SFT（有监督微调）**、**RLHF/RLAIF（基于人类/AI反馈的强化学习）**、**DPO（直接偏好优化）** | SFT 教会模型"听指令、按格式输出"；RLHF/DPO 让输出更符合人类偏好（有用、无害、诚实）；后训练一般不会显著新增知识，更多是对齐和激发已有能力 |
| 数据蒸馏（Distillation） | 用强模型（教师）的输出或概率分布指导弱/小模型（学生）训练，实现能力迁移与压缩 | 面试常见场景：用 GPT-4/Claude 等强模型合成 SFT 训练数据蒸馏到小模型；核心价值是降低推理成本、加速部署 |
| 大模型推理（Inference） | 模型训练完成后，加载权重对输入做自回归逐 token 生成的过程 | 与训练的本质区别：推理不更新参数，关注点是延迟、吞吐、成本 |
| Scaling Law | 描述模型性能与参数量、数据量、算力三者之间近似幂律关系的经验规律 | 两个阶段：训练时 Scaling Law（如 Chinchilla 提出"计算最优"下模型规模和数据量应同步增长）；推理时 Scaling Law / Test-time Compute（如 o1 类推理模型，通过增加推理阶段计算量/思考长度换取更高准确率） |
| 智能体（Agent） | 承接下一模块，此处仅作概念引出 | 一句话：Agent = LLM（大脑）+ 感知 + 规划 + 记忆 + 工具调用，能自主完成多步任务 |

#### 1.2 关键问题解答

**Q1：人工智能、传统 NLP、大模型、大语言模型的关系是什么？**
包含关系：AI 是最上位的学科概念；传统 NLP 是 AI 在语言领域的早期任务导向解法（规则/统计/浅层网络）；大模型是深度学习发展到规模化、自监督预训练阶段后出现的通用基座；大语言模型是大模型中专注文本/语言任务的分支。可以理解为：传统 NLP 是"手工作坊"（一任务一模型），LLM 是"通用工厂"（一模型多任务）。

**Q2：大语言模型的运作原理是什么？**
本质是一个基于 Transformer 的自回归条件概率模型：给定前面的 token 序列，预测下一个 token 的概率分布，并不断采样/选取下一个 token 拼接回输入，循环直到生成结束符或达到长度限制。所有"智能"表现都来自于在海量数据上学到的、极其复杂的下一词预测能力。

**Q3：大语言模型的组成是什么？有哪些关键部件？**
输入侧：Tokenizer 分词 + Embedding 层；主体：N 层堆叠的 Transformer Block，每层包含 Multi-Head Self-Attention（捕捉 token 间关系）、Feed-Forward Network（非线性变换、存储知识）、残差连接与归一化（稳定训练）；此外还需要位置编码注入顺序信息；输出侧：线性层将隐藏状态映射回词表维度，Softmax 得到下一 token 概率分布。

**Q4：什么是大模型预训练？**
在海量、多来源、基本无人工标注的语料上进行自监督训练，核心目标函数通常是 Next Token Prediction（也有 Masked LM 等变体用于 Encoder 类模型）。目的是让模型学习语言的统计规律、事实性知识、初步的推理和世界模型，是整个训练流程中算力消耗最大、耗时最长的阶段。

**Q5：什么是大模型后训练？有哪些关键技术？**
后训练 = 预训练之后、让模型"可用、好用、安全"的一系列对齐技术。关键技术：
- **SFT（Supervised Fine-Tuning）**：用高质量的"指令-回答"对做有监督训练，教会模型遵循指令、输出格式；
- **RLHF（基于人类反馈的强化学习）**：训练一个奖励模型（Reward Model）拟合人类偏好，再用 PPO 等强化学习算法优化策略模型；
- **RLAIF**：用 AI（而非人类）打分/生成偏好数据替代部分人工标注，降低成本；
- **DPO（Direct Preference Optimization）**：跳过显式奖励模型，直接用偏好对数据优化模型，训练更简单稳定，是近年工业界的热门替代方案。

**Q6：大模型推理有哪些工程优化手段？**【补充，属于测试测开必备工程常识】
- **KV Cache**：缓存已计算的 Key/Value，避免重复计算历史 token 的注意力；
- **量化（Quantization）**：INT8/INT4 等低精度权重/激活值，降低显存和计算开销；
- **PagedAttention / Continuous Batching**（如 vLLM）：提升显存利用率和并发吞吐；
- **Flash Attention**：IO 感知的注意力算子优化，降低显存读写开销；
- **投机采样（Speculative Decoding）**：用小模型先"打草稿"、大模型验证，加速生成；
- 面试关注指标：**TTFT（首 Token 时延）**、**TPOT（每 Token 生成时间）**、吞吐（Tokens/s）、并发数。

**Q7：大模型训练有哪几个阶段？有什么作用？**
预训练（学知识和语言能力，成本最大）→ SFT（学指令遵循和输出格式）→ 偏好对齐（RLHF/DPO，学"人类喜欢什么"，兼顾有用性/安全性）。有的团队还会插入"继续预训练/领域适应预训练"阶段来注入垂直领域知识。

**Q8：数据蒸馏的原理是什么？**
让学生模型不仅学习真实标签，还学习教师模型输出的概率分布（软标签）或直接学习教师模型生成的高质量文本/推理链，从而以更小的参数量逼近教师模型的能力。工业界常见做法是用强模型批量生成合成训练数据（如带 CoT 的问答对）去训练/微调小模型。

**Q9：为什么需要推理扩展？推理阶段如何扩展？**
预训练阶段的 Scaling Law 边际收益在放缓、成本急剧上升，"推理时扩展（Test-time Scaling）"提供了另一条路径：让模型在回答问题时"多想一会儿"（更长的思维链、自我反思、多路径采样再投票/验证），用推理阶段的算力换取准确率提升，代表是 OpenAI o1、类似的"推理模型"。常见扩展方式：延长 CoT 长度、Self-Consistency（多次采样投票）、树搜索（ToT）、生成后自我验证/反思。

**Q10：智能体的基本概念。**
见模块 3。一句话过渡：Agent 是把 LLM 从"一次性问答"升级为"能感知环境、自主规划、调用工具、维护记忆、多轮执行复杂任务"的系统。

#### 1.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：把"大模型"等同于"LLM"，面试官追问时要能说清楚大模型也包括图像/多模态基座模型，LLM 只是其中语言方向的子集。
- **易错点**：认为后训练会给模型"新增知识"——更准确的说法是后训练主要做对齐和能力激发，新知识注入效果有限（想大规模注入新知识通常还是要靠继续预训练或 RAG）。
- **高频追问**：RLHF 和 DPO 的区别？（RLHF 需要显式训练奖励模型+PPO 强化学习，流程复杂、训练不稳定；DPO 把偏好优化转化为一个简单的分类式损失函数，直接在偏好对上优化策略模型，工程实现更简单。）
- **测试测开视角**：预训练/后训练阶段测试关注"离线评测集+人工标注"；推理阶段测试关注"性能压测（并发/时延/吞吐）+ 正确性回归"；对测开而言，Scaling Law 提示我们评测集也需要"分层"（不同规模模型用不同难度基准，避免小模型分数长期为 0 而无区分度，或大模型分数饱和无区分度）。

---

### 模块 2：数据工程与 DCAI

#### 2.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
|---|---|---|
| DCAI（Data-Centric AI，以数据为中心的人工智能） | 一种 AI 研发范式：保持模型架构/代码基本不变，通过系统性提升数据的质量、一致性和覆盖度来提升整体系统效果 | 与 Model-Centric AI（死磕模型结构和调参）相对；核心口号"垃圾进、垃圾出"，数据质量往往比模型改进带来更大收益 |
| 数据工程（Data Engineering） | 构建、维护数据采集、存储、传输的基础设施和管道（Pipeline），保证数据可靠、高效地流动 | 关注系统稳定性、可扩展性、数据一致性，产出是"可用的数据管道"而非分析结论 |
| 数据分析（Data Analysis） | 对已有数据进行探索、统计、可视化，发现规律并支撑业务决策 | 偏"解释过去"，输出通常是报告/看板 |
| 数据科学（Data Science） | 综合统计学、机器学习、领域知识，从数据中挖掘价值、构建预测/决策模型 | 范围最广，涵盖数据工程+数据分析+建模全链路，偏"预测未来" |
| 数据管道（Data Pipeline） | 数据从产生到最终可用（分析/训练）过程中，经过采集、传输、转换、存储各环节串联起来的自动化流程 | 面试关注：管道的可观测性、容错、幂等性、数据血缘（Lineage） |
| ETL / ELT | 两种数据处理流程范式 | ETL（Extract-Transform-Load）：先在处理层完成转换清洗，再加载进目标仓库，适合传统数据仓库、结构固定的场景；ELT（Extract-Load-Transform）：先把原始数据加载进数据湖/云仓库，按需再转换，更灵活、更适合海量非结构化数据和现代云原生架构 |
| 数据清洗与数据预处理 | 数据清洗聚焦"修正数据本身的错误"：去重、缺失值处理、异常值处理、格式统一；数据预处理在清洗基础上"让数据适配模型"：归一化/标准化、编码转换（如 One-hot）、特征工程 | 二者常被混用，但面试可以清楚区分定位：清洗解决"数据对不对"，预处理解决"数据适不适合喂给模型" |
| 探索性数据分析（EDA） | 在正式建模前，通过统计摘要和可视化手段初步了解数据分布、异常、相关性的过程 | 常用手段：描述性统计、直方图、箱线图、相关系数矩阵、缺失值热力图 |

> 【修正】原资料数据工程小节的编号中缺少第 8 项（1-7、9 连续跳过 8），推测为誊抄遗漏，内容上未发现实质缺失，此处按逻辑顺序重新整理，未编号。

#### 2.2 关键问题解答

**Q1：DCAI 的定义是什么？DCAI 里不变的是什么？主要改进什么？**
DCAI 是一种"以数据为核心"而非"以模型为核心"的 AI 研发理念：在模型架构、代码基本保持不变的前提下，通过系统化地提升训练/评测数据的质量（一致性、标注准确度、覆盖度、去噪去偏）来提升系统整体表现。"不变"的是模型代码/架构，"改进"的是数据本身的质量和构成。

**Q2：数据质量对 AI 有哪些影响？**
数据质量直接决定模型能力上限和行为偏差：噪声数据会拉低模型准确率并引入不稳定性；标注不一致会让模型学到矛盾的信号；数据分布偏差（Bias）会导致模型输出有偏、不公平甚至产生幻觉；重复/污染数据（评测集混入训练集）会导致评测结果虚高、失真。

**Q3：数据工程、数据分析、数据科学有何区别？各自职责是什么？**
数据工程负责"管道"——把数据可靠地采集、传输、存储好；数据分析负责"洞察"——基于已有数据回答业务问题、生成报告；数据科学范围最广，负责"建模/预测"——综合统计和机器学习方法从数据中构建可复用的预测/决策系统，往往依赖前两者提供的高质量数据。

**Q4：数据采集的主要来源和方法有哪些？**【补充】
来源：业务日志埋点、爬虫抓取公开网页、公开数据集、用户主动标注/反馈、传感器/设备数据、第三方 API/数据供应商。方法上要关注采集的合规性（隐私/版权）、代表性（是否覆盖长尾场景）和时效性。

**Q5：ELT 和 ETL 分别是什么含义？二者有何区别？**
见 2.1 表格。核心区别在于"转换发生的时机"：ETL 先转换后加载，适合结构化、变化少的传统数仓；ELT 先加载后转换，适合海量、多样、变化快的现代大数据/云仓库场景，转换逻辑可以按下游需求灵活调整而不用重新采集。

**Q6：什么是数据清洗和预处理？二者定位各是什么？其常见的数据清洗和预处理方法有哪些？**
定位见 2.1。常见清洗方法：去重、缺失值填补/删除、异常值检测（如 3σ 原则、箱线图法）、格式/编码统一；常见预处理方法：归一化/标准化（Min-Max、Z-score）、类别特征编码（One-hot、Label Encoding）、文本分词与向量化、特征选择/降维。

**Q7：常见数据科学流程有哪些环节？**
问题定义 → 数据采集 → 数据清洗与 EDA → 特征工程 → 模型训练与选择 → 模型评估 → 部署上线 → 监控与迭代（含数据回流，见模块 9）。

**Q8：常见的数据分析方法有哪些？**
按目的分：描述性分析（发生了什么）、诊断性分析（为什么发生）、预测性分析（将会发生什么）、规范性分析（应该怎么做）；常用具体手法：对比分析、趋势分析、漏斗分析、归因分析、群组分析（Cohort）。

**Q9：常见的数据可视化方法有哪些？**
折线图（趋势/时间序列）、柱状图（类别对比）、饼图（占比构成，类别不宜过多）、散点图（两变量相关性）、热力图（矩阵型数据/相关系数）、箱线图（分布与异常值）。

#### 2.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：把"数据清洗"和"数据预处理"完全等同，面试官会追问二者边界。
- **高频追问**：为什么现代大模型/大数据场景更偏爱 ELT 而不是 ETL？（原始数据保留完整、转换逻辑可迭代、云存储成本低、支持多样化下游用途包括训练大模型）
- **测试测开视角**：作为测开，数据管道本身也是被测对象——需要对数据管道做**数据质量校验（Data Validation）**：Schema 校验、空值率/重复率监控、分布漂移（Data Drift）检测；这与后面 LLM/Agent 测评中的"数据回流"是同一套工程能力的复用。

---

### 模块 3：AI Agent 基础与架构

#### 3.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
|---|---|---|
| AI 智能体（Agent） | 能够感知环境、自主规划、调用工具、维护记忆，以完成给定目标的自动化系统，LLM 通常作为其"大脑/推理引擎" | 公式化理解：Agent ≈ LLM + Planning + Memory + Tool Use + Perception |
| 环境感知能力 | Agent 获取外部信息（文本、图像、API 返回、传感器数据等）以了解当前状态的能力 | 常见感知方式：文本输入、多模态输入（图像/语音）、结构化数据（数据库/API返回）、工具执行结果反馈 |
| 工具调用能力 | Agent 判断何时需要、需要调用哪个外部工具（搜索、代码执行、数据库查询等），并正确构造调用参数的能力 | 面试关注：工具选择准确率、参数构造正确率、工具调用失败的容错处理 |
| 记忆能力 | Agent 保存和利用历史信息（上下文、过往交互、长期知识）的能力 | 分短期/长期，见下 |
| 视觉语言模型（VLM） | 同时理解图像和文本、进行跨模态推理的模型 | Agent 感知能力扩展到视觉的关键底座，如 GUI Agent 依赖 VLM 理解屏幕截图 |
| 思维链（CoT，Chain-of-Thought） | 引导模型在给出最终答案前先输出中间推理步骤 | 提升复杂推理任务准确率的核心手段，是 ReAct 中"Thought"部分的理论基础 |
| ReAct 模式 | Reasoning + Acting 交替进行的 Agent 执行范式 | 循环：Thought（推理下一步做什么）→ Action（调用工具）→ Observation（获得工具返回结果）→ 回到 Thought，直到得出最终答案 |
| Reflection（反思）模式 | Agent 在执行后对自己的输出/过程进行自我评估和批判，并据此修正后续行为的模式 | 代表工作 Reflexion；核心价值是把"错误"转化为"经验"存入记忆，提升多轮任务成功率，区别于 ReAct 的"边做边想"，Reflection 更侧重"做完再复盘" |
| 短期记忆 | 存在于当前对话/任务上下文窗口内的临时信息 | 受 Context Window 长度限制，对应"工作记忆" |
| 长期记忆 | 跨会话持久化保存的信息，通常存储在外部（向量库/数据库） | 需要通过检索（常结合 RAG）按需取用，而非全部放入 Prompt |
| MCP（Model Context Protocol） | 标准化 LLM 应用连接外部数据源和工具的开放协议 | 解决"每个工具都要写一套定制适配代码"的碎片化问题，让 Agent 与工具之间"即插即用" |
| A2A（Agent2Agent Protocol） | 面向 Agent 与 Agent 之间通信、协作、互操作的协议 | 解决跨厂商、跨框架的 Agent 之间如何互相发现能力、交换任务和结果的问题 |
| ANP（Agent Network Protocol） | 面向去中心化场景的智能体网络协议，关注智能体身份、发现与安全通信 | 更强调开放网络环境下 Agent 的身份认证与去中心化协作，与 MCP（Agent-工具）、A2A（Agent-Agent，多为受控/企业场景）形成互补 |
| AI Agent 开发范式 | 根据项目需求选择的两种典型构建方式 | ① **单体/工作流式 Agent**：基于预定义流程（Workflow/状态机）编排，可控性强、适合任务边界清晰的场景；② **自主智能体（Autonomous Agent）**：模型自主决定行动路径（如纯 ReAct 循环），灵活性强但可控性和可预测性较弱，适合开放式、探索型任务 |

#### 3.2 关键问题解答

**Q1：什么是 AI 智能体？**
AI 智能体是以大模型为推理核心，具备感知环境、自主规划任务步骤、调用外部工具、维护短期/长期记忆能力，能够多轮、自主完成复杂目标的系统，区别于传统"一问一答"的 LLM 应用。

**Q2：AI Agent 的关键特征有哪些？**
自主性（无需每步都由人类指令）、目标导向（围绕给定目标规划行动）、环境交互性（能感知反馈并调整）、工具使用能力、记忆能力（能利用历史信息）。

**Q3：Agent 的典型任务执行流程是什么？**
接收目标 → 感知/理解当前状态与上下文 → 规划（拆解子任务/决定下一步行动）→ 执行（调用工具或直接生成回复）→ 观察执行结果 → 判断是否达成目标，否则回到规划阶段迭代，直至完成或达到终止条件。

**Q4：怎么理解 AI Agent 的关键技术能力？**
可以归纳为四大能力支柱：**规划能力**（把模糊目标拆解成可执行步骤，CoT/ReAct/ToT 等都是规划技术）、**工具使用能力**（把"想法"转化为"行动"，调用搜索/代码/API）、**记忆能力**（让 Agent 在长任务/多会话中保持连贯性和可积累经验）、**感知能力**（理解多模态/结构化输入，是与外部世界对齐的基础）。

**Q5：AI Agent 常用的感知方式有哪些？**
文本理解（自然语言指令/文档）、多模态感知（图像/语音，依赖 VLM）、结构化数据感知（数据库查询结果、API 返回的 JSON）、工具执行反馈（Observation）。

**Q6：触发模型推理有哪三种常见模式？**
① **CoT**：一次性输出完整思维链再给答案，无外部交互；② **ReAct**：思考与行动交替，边推理边调用外部工具获取新信息；③ **Reflection**：在得到初步结果后进行自我评估/批判，再修正输出（可以叠加在 CoT/ReAct 之上作为后处理循环）。

**Q7：ReAct 模式的交互流程是怎样的？**
Thought（模型思考"当前该做什么/为什么"）→ Action（模型输出要调用的工具及参数）→ 执行工具 → Observation（工具执行结果反馈给模型）→ 模型基于新的 Observation 继续下一轮 Thought，如此循环直至模型认为已经收集到足够信息，输出 Final Answer。

**Q8：常见的四种类型记忆有哪些？**【补充：面试中常引用认知架构（如 CoALA）的记忆分类】
- **工作记忆（短期记忆）**：当前上下文窗口内的信息，任务/会话结束即消失；
- **情景记忆（Episodic Memory）**：过去具体交互事件/经历的记录（"上次用户问了什么、Agent做了什么"）；
- **语义记忆（Semantic Memory）**：抽象化的事实性知识（用户偏好、领域知识库），通常存于向量库供检索；
- **程序记忆（Procedural Memory）**：如何做事的"技能/流程"，例如成功的工具调用序列、Prompt 模板本身。

**Q9：为什么需要 MCP 协议？**
在 MCP 出现之前，每接入一个新工具/数据源，开发者都需要为该工具单独写适配代码、单独设计 Prompt 描述其用法，导致"M 个模型 × N 个工具"的组合爆炸式适配成本。MCP 提供一套标准化协议，让工具/数据源以统一方式描述自身能力，Agent 侧只需实现一次 MCP 客户端即可对接任意 MCP Server，实现"即插即用"，降低集成成本、提升可复用性。

**Q10：为什么需要 Multi-Agent 模式？**
单一 Agent 在处理复杂、跨领域、需要长链条协作的任务时容易出现：上下文过载（一个 Agent 要记住所有领域知识和历史）、角色混淆（既要写代码又要审代码，缺乏"旁观者视角"）、并行度低（所有子任务串行处理）。Multi-Agent 通过"分工"让每个 Agent 专注单一角色/领域，天然支持并行执行和交叉校验（如一个 Agent 生成、一个 Agent 审查），提升复杂任务的整体质量和效率。

**Q11：如何使用 Multi-Agent 模式？**
明确任务边界，设计角色分工（如 Planner/Executor/Reviewer 或按领域拆分）；设计 Agent 间的通信协议和数据交换格式（可用 A2A 等标准协议或自定义消息结构）；设计编排架构（见 Q13）；设计终止/汇总机制，由某个 Agent（通常是 Orchestrator）负责整合各子 Agent 结果并输出最终答案。

**Q12：Multi-Agent 之间通过什么协议协作？**
主要依赖 **A2A（Agent2Agent Protocol）** 实现跨 Agent 的能力发现、任务委派与结果交换；在更开放、去中心化的网络环境下则可能涉及 **ANP** 处理身份认证和安全通信；Agent 与外部工具/数据源之间则是 **MCP** 的职责——三者常被放在一起对比，但服务对象不同：MCP（Agent↔工具/数据）、A2A（Agent↔Agent，多为受控/企业场景协作）、ANP（Agent↔Agent，偏开放网络的身份与发现）。

**Q13：Multi-Agent 的协作模式包括哪三种架构？**
① **集中式/编排式（Orchestrator-Worker）**：有一个中心 Agent 负责任务拆解、分发和结果汇总，其余 Agent 是"执行者"，可控性强，是当前工业界最主流的落地方式；② **层级式（Hierarchical）**：多层级的编排结构，上层 Agent 管理下层 Agent 团队，适合超大规模复杂任务；③ **去中心化/对等式（Decentralized / Peer-to-Peer）**：Agent 之间平等协商、直接通信，无固定中心节点，灵活性最高但协调开销和不可预测性也最大。

**Q14：如何从零构建一个 Agent？**
① 明确任务目标与边界（能做什么、不能做什么）；② 选择基座模型；③ 设计系统 Prompt/认知框架（角色、目标、约束）；④ 设计并接入工具集（明确工具 Schema，最好走 MCP 标准）；⑤ 设计记忆机制（短期用上下文管理，长期用向量库/RAG）；⑥ 设计执行循环（ReAct/工作流编排）；⑦ 加入安全护栏（权限控制、输出校验、人工确认节点）；⑧ 构建评测集并持续测试迭代（对应模块 8/9）。

**Q15：根据项目需求，AI Agent 有哪两种开发范式？**
见 3.1 表格最后一行：**工作流编排式（Workflow-based）** vs **自主智能体式（Autonomous/ReAct-based）**。前者可控性强、适合流程边界清晰的业务场景（如客服工单处理）；后者灵活性强、适合探索性/开放式任务（如通用编程助手），但需要更强的护栏和评测投入。

**Q16：尽管前景广阔，但 AI 建设在实际落地中仍然面临严峻考验，你能说一说吗？**
（面试可结合测开视角展开）常见挑战：① **幻觉与不可靠性**——多步任务中误差会累积放大；② **可控性与安全性**——自主 Agent 的行为边界难以完全预测，需要 Harness/护栏工程兜底；③ **评测困难**——开放式任务缺乏标准答案，人工评测成本高，裁判模型本身也可能有偏差；④ **成本与延迟**——多步推理+多次工具调用带来的 Token 消耗和时延对用户体验和 ROI 是挑战；⑤ **工具生态碎片化**——虽有 MCP/A2A 等协议在推进标准化，但实际生产环境中工具接入、权限管理仍然复杂；⑥ **长期记忆与个性化**——如何低成本、准确地维护和更新长期记忆仍是工程难题。

#### 3.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：把 ReAct 和 Reflection 混为一谈——ReAct 强调"边推理边行动"的循环，Reflection 强调"事后自我批判再修正"，二者可以叠加使用但解决的问题不同。
- **易错点**：认为 MCP/A2A/ANP 是竞争关系——更准确的理解是三者面向不同的连接对象（工具、受控 Agent 协作、开放网络 Agent 协作），在实际系统中可能同时存在。
- **高频追问**：Multi-Agent 一定比单 Agent 好吗？（不一定，需要权衡协调开销、通信成本、错误传播风险；简单任务用 Multi-Agent 反而可能"杀鸡用牛刀"，增加不确定性和延迟。）
- **测试测开视角**：Agent 的可测性天然弱于传统软件——同样输入不保证同样输出（非确定性）、中间步骤（工具调用序列）比最终答案更值得被测试和断言、需要为每个"能力支柱"（规划/工具/记忆）单独设计评测维度而不能只看端到端结果（见模块 8）。

---

### 模块 4：Prompt Engineering

#### 4.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
|---|---|---|
| 提示词工程（Prompt Engineering） | 通过设计和优化输入给模型的文本（Prompt），在不改变模型参数的前提下引导模型输出符合预期结果的技术 | 成本最低、迭代最快的模型能力"调用"方式，是 RAG/Agent 的基础工程手段 |
| 专业 Prompt 四要素 | 写好一个高质量 Prompt 通常需要包含的四类信息 | 常见框架（如 RTF / CO-STAR 思路）：**角色（Role）**——赋予模型身份视角；**任务/指令（Task/Instruction）**——明确要做什么；**背景/上下文（Context）**——提供必要信息；**输出格式/约束（Format/Constraints）**——规定输出形式、长度、风格、禁止项 |
| 动态示例选取（Dynamic Few-shot Selection） | 根据当前输入，从示例库中检索最相关的少量示例动态拼入 Prompt，而非使用固定不变的示例集 | 相比静态 Few-shot，动态选取能提升示例与当前任务的相关性，从而提升效果，常通过 Embedding 相似度检索实现 |
| 自动提示工程（APE，Automatic Prompt Engineering） | 用模型自身（或另一个模型）自动生成、评估、迭代优化 Prompt 的技术 | 把"人工调 Prompt"变成"用算法/模型搜索最优 Prompt"，常结合评测集做闭环优化 |
| 多模态信息提取 | 从图像、文档扫描件等非纯文本输入中，通过设计合适提示词引导（V）LM 抽取结构化信息 | 三大核心技巧【补充，结合常见实践】：① 明确输出 Schema（如要求输出 JSON 并给出字段定义）；② 提供少量示例锚定格式和粒度；③ 分步骤引导（先定位区域/元素，再逐项提取，降低一次性提取的错误率） |
| 多模态检索 | 支持跨图文模态的语义检索，如以文搜图、以图搜文 | 依赖多模态 Embedding（如 CLIP 类模型）把图像和文本映射到同一向量空间 |
| 生成式 Prompt | 用于引导模型"创造"新内容（写作、代码生成、创意任务）的提示词 | 关注开放性、多样性引导（如给风格/受众/篇幅约束） |
| 检索式 Prompt | 用于引导模型基于给定/检索到的资料作答，而非凭"记忆"自由发挥（RAG 场景的核心提示词模式） | 关注"忠实性"约束（要求模型只依据提供的资料回答、明确标注信息来源、资料不足时如何回应），是抑制幻觉的关键手段 |

#### 4.2 关键问题解答

**Q1：什么是 Prompt？**
Prompt 是用户/系统输入给大模型、用于引导其生成特定输出的文本（也可包含图像等多模态内容），是人类与大模型交互的"接口语言"。

**Q2：Prompt 有哪些典型应用场景？**
问答/对话、内容生成（写作、代码）、信息抽取与结构化、分类/打标、翻译、摘要、Agent 系统提示（角色设定+工具描述）、RAG 中的检索结果整合提示。

**Q3：写好一个 Prompt 的四要素。**
角色、任务、背景/上下文、输出格式与约束（见 4.1）。

**Q4：列举优化 Prompt 的几个方向。**
明确化（消除歧义指令）、结构化（用分隔符/编号拆分不同信息块）、示例化（Few-shot，尤其是动态示例选取）、分步化（引导 CoT，让模型先想后答）、约束化（限定输出格式/长度/禁止项，便于下游解析和测试断言）、迭代化（基于评测集反馈持续调整，即人工版的 APE）。

**Q5：Prompt 开发过程中如何立角色、定目标、补要求？**
立角色：给模型一个具体、有边界感的身份（"你是一名资深 XX 专家"），有助于对齐输出的专业度和视角；定目标：用清晰、可衡量的语言描述期望产出（避免"帮我看看这个"这类模糊指令）；补要求：显式列出格式、篇幅、语气、必须/禁止包含的内容、边界情况（如信息不足怎么办）等约束条件，减少模型"自由发挥"带来的不确定性。

**Q6：常见的 Prompt 分隔符有哪些？**
三引号 `"""`、XML/类 XML 标签（如 `<context>...</context>`）、Markdown 标题/代码块、`###` 或 `---` 分隔线。作用是让模型清晰区分指令、背景资料、示例、用户输入等不同信息块的边界，降低"指令注入/混淆"风险，XML 标签在长上下文和需要模型精确引用来源时尤其常用。

**Q7：图像生成的 Prompt 三要素是什么？**【补充，属常见图像生成实践总结】
主体描述（画什么，对象/人物/场景）、风格与技法（如摄影风格、艺术流派、渲染方式）、构图与细节参数（视角、光线、色调、分辨率等约束词）。

**Q8：设计多模态信息提取提示词的三大核心技巧是什么？**
见 4.1 表格中"多模态信息提取"一行的三点。

**Q9：为什么多模态提示词重要？**
真实世界的信息往往是图文/音视频混合的（发票、报表截图、UI 界面、图表），纯文本 Prompt 无法覆盖这些场景；设计良好的多模态提示词能让模型更准确地在跨模态信息中定位、理解并输出结构化结果，是 GUI Agent、文档理解类应用的核心能力基础。

**Q10：图文混合多模态检索提示词设计的核心原则是什么？**
① 明确检索目标的模态（以文搜图/以图搜文/图文联合检索）；② 保证 Query 和被检索内容映射到同一语义空间的表达一致性（避免 Query 过于抽象而图像描述过于具体导致的语义鸿沟）；③ 对检索结果的后续使用给出明确指示（如"结合以下检索到的图片描述回答问题，若图片与问题无关请说明"），避免模型强行编造图文关联。

#### 4.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：把"检索式 Prompt"简单理解为"把资料贴进去就行"——面试官会追问如何降低模型"绕过资料凭记忆回答"的风险，答案是显式的忠实性约束+来源标注要求+"资料不足时明确说明"的兜底指令。
- **高频追问**：动态 Few-shot 选取和 APE 的关系？（动态 Few-shot 解决"选哪些示例"，APE 解决"Prompt 本身怎么写/怎么自动迭代"，二者可以结合：用 APE 搜索最优的 Prompt 模板骨架，用动态选取决定填充哪些示例。）
- **测试测开视角**：Prompt 本身要能被**版本化管理和回归测试**——同一 Prompt 模板在模型升级/参数微调后应有一套标准化评测集跑分对比（Prompt Regression Testing），格式/约束类要求应设计为可自动化断言的形式（如强制 JSON 输出便于程序校验，而非依赖人工肉眼检查）。

---

### 模块 5：Context Engineering

> 【修正】原资料写"回答 5 个问题"，但实际只列出了 4 个问题；此处按资料实有的 4 题整理，并在末尾补充 1 道面试常见追问题标注【补充】，未编造具体资料内容。

#### 5.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
|---|---|---|
| Context Engineering（上下文工程） | 系统性地设计和管理"喂给模型的全部信息"（指令、检索结果、工具输出、历史记忆等），比 Prompt Engineering 范畴更广、更动态 | 关系：Prompt Engineering 关注"这一次怎么写"，Context Engineering 关注"整个系统运行过程中该给模型看什么、什么时候给、给多少" |
| 上下文压缩（Context Compression） | 在上下文接近或超出窗口限制时，通过摘要、筛选、丢弃等手段减少 Token 占用同时尽量保留关键信息 | 目的：控制成本和延迟、避免"Lost in the Middle"（长上下文中间部分信息被模型忽略的现象） |
| 记忆和摘要（Memory & Summarization） | 用摘要的方式把冗长的历史对话/工具输出压缩为精炼要点，作为长期记忆的一种存储形式 | 摘要本身也可能丢失细节，需要在"压缩率"和"信息保真度"之间权衡，关键事实/决策类信息通常需要结构化单独保留而非仅靠自然语言摘要 |
| 压缩时机 | 何时触发上下文压缩的策略问题 | 常见触发点：① 上下文接近窗口容量上限（如达到 80% 阈值）；② 每完成一轮工具调用/子任务后主动摘要；③ 会话中的自然节点（话题切换、任务阶段完成） |
| 过滤（Filtering） | 在信息进入上下文前，先剔除与当前任务不相关或低价值的内容 | 与压缩的区别：压缩是"保留但缩短"，过滤是"直接不放入"；常用于工具返回结果过大时先做相关性筛选 |
| Agentic Context Engineering | 在多 Agent/长流程 Agent 系统中，围绕"任务执行链路"设计上下文管理策略的工程实践 | 核心议题包括 Sub-Agent 之间的上下文是否共享/隔离、Orchestrator 如何汇总/精简子任务结果后再传递，避免上下文"污染"和无关信息累积 |

#### 5.2 关键问题解答

**Q1：什么是 Context Engineering？**
Context Engineering 是围绕"模型在每次推理时实际能看到什么信息"进行系统性设计的工程实践，涵盖系统提示、对话历史、检索到的知识、工具调用结果、长短期记忆等所有进入上下文窗口的内容的组织、筛选、压缩与呈现方式，目标是在有限的上下文窗口和成本预算下，让模型看到"最相关、最必要"的信息。

**Q2：Context Engineering 实现方法有哪些？分别有什么作用？**
① **检索（Retrieval，常结合 RAG）**：按需从外部知识库取相关片段，避免把全部知识塞进 Prompt；② **摘要/压缩**：把冗长历史或工具输出精炼为要点，节省 Token；③ **过滤**：在放入上下文前剔除不相关/低价值内容；④ **结构化组织**：用分隔符/角色标签清晰划分不同信息来源，降低模型混淆风险；⑤ **记忆分层管理**：区分短期（当前上下文）和长期（外部存储、按需检索）记忆，避免所有历史都堆在上下文里。

**Q3：压缩时的注意事项有哪些？大模型什么时候开始压缩？**
注意事项：压缩不能丢失关键决策/事实性信息（尤其是任务约束条件、用户明确给出的偏好）；压缩应尽量结构化（如保留关键实体和结论、而非纯自然语言泛泛概括）；压缩本身有成本（多一次模型调用），需要权衡触发频率。触发时机：接近上下文窗口容量阈值时（预留安全余量，而非等到超限才处理）；完成一个阶段性子任务/工具调用批次后主动摘要，防止历史无限累积；长会话中自然的话题/任务切换点。

**Q4：Sub-Agent 的 Context 是相互隔离的吗？**
在大多数主流 Multi-Agent 架构设计中，**Sub-Agent 之间的上下文通常是相互隔离的**：每个 Sub-Agent 只维护自己任务范围内的上下文，避免被其他 Agent 的无关历史"污染"，从而保持推理聚焦、降低 Token 成本；Sub-Agent 完成任务后，通常只把**精简后的结果（而非完整上下文）**返回给 Orchestrator（编排 Agent），再由 Orchestrator 决定是否以及如何将其纳入全局上下文。也存在共享内存/黑板模式（Shared Memory / Blackboard）的架构，多个 Agent 可以读写同一个共享上下文区域，但这不是默认做法，而是针对需要紧密协作场景的特定设计选择。

**Q5（【补充】常见面试追问，非资料原题）：Context Engineering 中容易踩的坑有哪些？**
过度压缩导致关键约束丢失（如用户明确说过的"禁止使用某方案"被摘要掉）；把不相关的历史/工具输出全部堆进上下文导致"Lost in the Middle"、模型注意力被稀释；压缩/摘要本身引入新的错误（模型摘要时"脑补"了不存在的信息）；对多 Agent 系统而言，Sub-Agent 结果未经过滤直接全量返回给 Orchestrator，造成上下文膨胀。

#### 5.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：把 Context Engineering 等同于"更长的 Prompt"——恰恰相反，其核心价值往往是"如何用更少、更精的信息达到同样甚至更好的效果"。
- **高频追问**：Context Engineering 和 RAG 是什么关系？（RAG 是 Context Engineering 里"如何获取外部知识放入上下文"的一种具体技术手段，Context Engineering 是更上层、更系统的管理视角，还包括压缩、过滤、记忆分层等 RAG 不直接涉及的议题。）
- **测试测开视角**：需要专门设计**长上下文/多轮压缩场景的回归用例**——验证压缩前后关键信息（用户约束、历史决策）是否被保留，可用"关键事实召回率"作为量化指标；对 Sub-Agent 隔离架构，需要测试"上下文泄漏"（Sub-Agent 是否意外获取了不应看到的信息）和"信息丢失"（Orchestrator 是否遗漏了 Sub-Agent 的关键结论）两类反向用例。

---

### 模块 6：RAG、微调与知识增强

#### 6.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
|---|---|---|
| RAG（Retrieval-Augmented Generation，检索增强生成） | 在生成回答前，先从外部知识库检索相关内容，再将其作为上下文提供给模型辅助生成 | 核心价值：让模型回答基于"外部最新/私有知识"而非仅依赖训练时"记住"的内容，降低幻觉、便于知识更新 |
| 微调（Fine-tuning） | 在预训练模型基础上，用特定任务/领域的数据继续训练，调整模型参数 | 与 RAG 的本质区别：微调改变模型"内部参数/行为风格"，RAG 改变模型"外部可见的知识来源"，二者可组合使用 |
| 提示工程 | 见模块 4 | 在 RAG/微调选型对比中作为"零改造成本"的基线方案 |
| 语义分片（Semantic Chunking） | 按照语义完整性（而非固定字符数）切分文档为若干片段，用于向量化和检索 | 相比固定长度切分，语义分片能减少"一个完整语义单元被硬切断"的问题，提升检索片段的可用性 |
| 向量化（Embedding） | 将文本片段转换为固定维度的稠密向量，使语义相近的内容在向量空间中距离相近 | 是实现"语义检索"（而非仅关键词匹配）的基础 |
| 检索（Retrieval） | 根据用户 Query 的向量，在向量库中查找语义最相似的若干文档片段 | 常用相似度度量：余弦相似度、内积；工程上常配合近似最近邻（ANN）索引（如 HNSW）加速大规模检索 |
| 混合检索（Hybrid Retrieval） | 将稀疏检索（如 BM25 关键词匹配）与稠密检索（向量语义匹配）结合，通常再融合排序 | 兼顾"精确关键词/专有名词匹配"（稠密检索容易漏召回的场景）和"语义相似但字面不同"的场景，是当前工业级 RAG 系统的主流做法 |
| 增强式（Augmentation） | RAG 流程中把检索到的内容与原始 Query 组织进最终 Prompt，供模型生成答案的环节 | 需要设计好的模板控制模型"如何使用"检索内容（忠实性约束、引用来源等，见模块 4 检索式 Prompt） |

#### 6.2 关键问题解答

**Q1：RAG 的定义。**
RAG 是一种让大模型在生成回答前先从外部知识库（结构化或非结构化）检索相关信息、并将检索结果注入 Prompt 辅助生成的技术框架，目的是弥补模型参数化知识的时效性和覆盖面不足，降低幻觉，同时支持私有/垂直领域知识的低成本接入。

**Q2：大模型的应用落地通常有哪四种常见的技术选型方案？**
① **纯 Prompt Engineering**：零训练成本，直接靠提示词引导通用模型；② **RAG**：外挂知识库，解决知识时效性/私有性问题；③ **微调（Fine-tuning，含 SFT/LoRA 等）**：改变模型内部行为，适合需要稳定输出风格/格式或深度领域适配的场景；④ **从零/继续预训练（Pre-training/Continued Pre-training）**：成本最高，通常只有基座模型厂商或需要注入大规模全新领域语料的场景才会选择。

**Q3：选 RAG 还是大模型微调，主要考虑哪五个关键点？**
① **知识时效性**：知识频繁更新选 RAG（更新知识库即可，无需重新训练）；② **任务性质**：偏"知识问答/查证事实"选 RAG，偏"风格/格式/领域推理模式固化"选微调；③ **成本与迭代速度**：RAG 迭代快、成本低，微调需要标注数据+训练算力，迭代周期长；④ **幻觉容忍度与可解释性**：RAG 可以提供可追溯的信息来源，可解释性更好；⑤ **数据隐私与合规**：私有数据用 RAG 可以做到"检索时才接触"、更易做权限控制，微调则需要把数据直接"烧入"模型参数。（实践中常见做法是两者结合：微调让模型学会遵循特定输出格式/更好地利用检索结果，RAG 提供实时知识。）

**Q4：RAG 的整体工作流程涉及哪几个关键步骤？**
文档解析 → 语义分片 → 向量化 → 存入向量数据库 → 用户 Query 向量化 → 检索（可结合混合检索）→ 重排序（Rerank，可选但工业界常用，用更精细的模型对粗召回结果精排）→ 增强（拼装 Prompt）→ 生成最终答案。

**Q5：文档解析（MRC）的定义和作用？**
【修正】此处"MRC"与标准 RAG 流程中的"文档解析"并非同一概念：MRC 通常指"机器阅读理解（Machine Reading Comprehension）"，是给定文档和问题、让模型直接从文档中定位/生成答案的任务，更偏"理解-问答"能力评测；而 RAG 流程中的"文档解析"是指将 PDF/Word/网页等原始文档转换为可处理的结构化/半结构化文本（含版式识别、表格提取、OCR 等）的**预处理环节**。二者关系可以理解为：文档解析是 RAG 的"输入清洗"步骤，为后续分片/向量化提供干净文本；而 MRC 更接近对"模型利用文档回答问题"这一最终能力的评测任务。面试时建议主动澄清这个概念差异，展现基础功底扎实。

**Q6：什么是语义分片？为什么需要语义分片？语义分片通常有哪几种方案？**
定义见 6.1。需要的原因：固定长度切分（如每 500 字符切一段）容易把一个完整的语义单元（如一段论述、一个表格）从中间切断，导致检索到的片段语义不完整、影响生成质量。常见方案：① 基于文档结构切分（按标题/段落/章节天然边界）；② 基于句子/语义相似度切分（计算相邻句子的语义相似度，在语义"断点"处切分）；③ 滑动窗口+重叠切分（在固定长度基础上保留片段间的重叠区域，缓解边界信息丢失）；④ 递归式/层级切分（先按大结构切，再对过长片段递归细分）。

**Q7：什么叫向量化？向量化带来哪些优势？**
向量化是把文本（或图像等）通过 Embedding 模型映射为固定维度的稠密数值向量的过程。优势：① 支持**语义检索**而非仅字面关键词匹配，能召回语义相关但字面不同的内容；② 向量之间可以用高效的数学方法（余弦相似度等）快速计算相似度，便于工程化大规模检索；③ 为跨模态检索（文搜图等）提供统一的表示空间。

**Q8：为什么需要混合检索？**
纯稠密（向量）检索在处理专有名词、精确数字、罕见词等场景时容易召回不准（语义相近但字面完全不同的内容可能被误召回，而字面完全匹配的关键内容反而可能因为语义向量距离稍远被漏召回）；纯稀疏（关键词）检索则无法处理同义表达、语义相似但用词不同的查询。混合检索通过融合两者（并常配合 Rerank 精排）取长补短，在工业实践中通常比单一检索方式召回质量更稳定。

**Q9：什么是大模型幻觉？**
大模型幻觉（Hallucination）指模型生成了看似流畅、自信，但与事实不符、缺乏依据，或与给定资料矛盾的内容。产生原因包括：预训练/后训练数据本身含错误或过时信息、模型在没有把握时仍倾向于"生成一个答案"而非承认不知道、生成过程本质是概率采样而非"查证事实"。RAG 是缓解幻觉的重要手段之一（让回答"有据可查"），但如果检索质量差或模型不忠实于检索内容，RAG 系统同样会产生幻觉。

**Q10：RAG 工作流程中的增强环节主要完成了什么功能？**
增强（Augmentation）环节负责把"检索到的原始知识片段"和"用户原始问题"按照设计好的 Prompt 模板组织成最终输入给模型的完整上下文，同时通常会附加忠实性约束指令（如"仅根据以下资料回答，资料未提及则明确说明未找到相关信息"）和引用来源的格式要求，其质量直接决定模型是否能"正确、忠实地"使用检索到的知识生成最终答案。

#### 6.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：认为"RAG 能完全消除幻觉"——更准确的说法是 RAG **降低**幻觉风险，但检索质量差（召回不准/召回不全）或生成时不忠实于检索内容依然会产生幻觉，因此需要专门测评"忠实性/Faithfulness"指标。
- **高频追问**：Rerank 的作用和必要性？（粗召回阶段为了效率通常召回 Top-K（K 较大）候选，Rerank 用计算成本更高但更精准的模型（如 Cross-Encoder）对这 K 个候选重新精细排序，取 Top-N 送入生成阶段，在准确率和效率之间取得平衡。）
- **测试测开视角**：RAG 系统需要**分环节评测**而非只看最终答案——检索环节看 **召回率（Recall@K）/精确率/MRR/NDCG**；生成环节看**忠实性（是否有据可查）、答案正确性、拒答能力**（资料不足时是否会诚实说明而非编造）；语义分片方案变化、Embedding 模型升级都需要对应的**检索质量回归测试**。

---

### 模块 7：Harness Engineering

#### 7.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
|---|---|---|
| Harness Engineering | 围绕大模型/Agent 构建的"工程脚手架"：定义模型如何被调用、能看到什么上下文、能使用哪些工具、循环如何控制、异常如何兜底的整套工程系统 | 与 Prompt/Context 的层级差异见 Q1；可以理解为"运行模型的操作系统/跑道" |
| 认知框架 | Harness 中用于规范模型"如何思考"的结构化约束，如固定的推理模板（先分析、再规划、再执行）、角色设定、决策准则 | 通过 System Prompt + 标准化推理模板 + 少量示例来"塑形"模型的思考方式，使其行为更可预测 |
| 能力边界 | Harness 中限定模型/Agent"能做什么、不能做什么"的机制 | 常见手段：工具白名单/权限分级、敏感操作强制人工确认（Human-in-the-loop）、输出内容校验与拦截、速率/资源限制 |
| 模型行为 | Agent 在具体运行中表现出来的决策序列和输出结果 | Harness 工程的最终目标就是让模型行为在概率性的本质下，依然表现出工程上可接受的稳定性和可控性 |

#### 7.2 关键问题解答

**Q1：Prompt、Context、Harness 三者有什么差异？**
三者是从"点"到"面"到"系统"层层扩大的概念：**Prompt** 是单次输入给模型的具体文本内容（"这一次说什么"）；**Context** 是模型在某一次推理时实际能看到的全部信息集合，包含 Prompt 本身、历史对话、检索结果、工具输出等（"这一次能看到什么"），由 Context Engineering 负责组织管理；**Harness** 则是包裹整个模型调用过程的工程系统，定义了模型如何被反复调用（循环/工作流控制）、上下文如何被组装（调用 Context Engineering 的产出）、可以使用哪些工具及其权限、遇到异常/边界情况如何处理、以及整体的安全护栏（"系统怎么跑、跑多久、出了问题怎么办"）。简言之：Harness ⊃ Context 的组织逻辑 ⊃ 单次 Prompt。

**Q2：如何控制认知框架？**
通过精心设计的 System Prompt 明确角色、目标、行为准则；通过标准化的推理模板（如强制要求"先分析问题类型，再判断是否需要工具，再执行"）约束模型的思考顺序；通过少量高质量的示例（Few-shot）锚定期望的推理风格；对更强的约束需求，可结合微调把某种"思考习惯"固化进模型参数，而非仅靠 Prompt 层面的软约束。

**Q3：如何控制能力边界？**
工具/权限分级：只暴露任务必需的最小工具集，对高风险操作（删除、支付、发送外部消息）设置单独的权限门槛；引入 **Human-in-the-loop**：关键节点强制人工审核/确认后才能继续执行；输出侧校验：对模型生成的行动指令（如 SQL、代码、API 调用参数）在真正执行前做静态校验/沙箱试跑；资源与速率限制：限制单次任务的最大步数、最大工具调用次数、最大 Token 消耗，防止无限循环或失控行为。

**Q4：如何用标准工作流程控制模型行为？**
将复杂任务拆解为预定义的阶段/状态机（而非完全交给模型自由发挥的开放式 ReAct 循环），每个阶段有明确的输入输出契约和校验规则，阶段之间的流转由确定性的工程逻辑（而非模型自主判断）控制；在关键阶段插入结构化的检查点（Checklist/断言），只有满足条件才能进入下一阶段；对必须由模型自主决策的部分，尽量收窄决策空间（如提供有限的选项而非完全开放式生成），从而在保留一定灵活性的同时提升整体行为的可预测性和可测试性。

#### 7.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：把 Harness 简单理解为"就是 Prompt 写得更长更细"——核心差异在于 Harness 包含大量**非 Prompt 的工程控制逻辑**（循环控制、权限系统、异常处理、状态机），这些是代码层面而非文本层面的约束。
- **高频追问**：为什么说"自主 Agent 需要更强的 Harness"？（自主性越强，模型自行决策的空间越大，行为的不可预测性也越高，因此需要更强的能力边界约束和更完善的异常兜底机制来把风险控制在可接受范围内，这是灵活性和可控性之间的经典权衡。）
- **测试测开视角**：Harness 本身是**高优先级的被测对象**——需要专门设计"边界测试用例"（尝试诱导模型突破权限/调用未授权工具，验证护栏是否生效）、"异常注入测试"（工具调用失败/超时/返回异常数据时，Harness 是否能优雅降级而非让整个 Agent 崩溃或进入死循环）、"最大步数/资源限制"测试（验证防失控机制真实生效）。

---

### 模块 8：Agent 评测 vs LLM 评测

#### 8.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
|---|---|---|
| 工具调用能力评测：NL2API | 评测模型将自然语言指令转换为正确 API 调用（含接口选择和参数构造）的能力 | 常用指标：API 选择准确率、参数填充准确率、端到端执行成功率 |
| NL2SQL | 评测模型将自然语言问题转换为正确 SQL 查询语句的能力 | 常用评测方式：**执行准确率（Execution Accuracy）**——执行生成 SQL 后结果是否与标准答案一致（比逐字符匹配更贴近实际可用性）；也可用精确匹配（Exact Match）但对 SQL 写法多样性不够宽容 |
| NL2Code | 评测模型将自然语言需求转换为可运行、正确代码的能力 | 常用指标：**Pass@k**（生成 k 次代码中至少有一次通过测试用例的比例）、单元测试通过率 |
| 记忆能力评测 | 评测 Agent 在多轮/跨会话场景中正确存储、检索、利用历史信息的能力 | 关注：关键信息召回率、长程一致性（是否会前后矛盾/遗忘用户明确说过的约束） |
| 规划能力评测 | 评测 Agent 把复杂目标拆解为合理、可执行步骤序列的能力 | 关注：计划的逻辑合理性、步骤间依赖关系是否正确、是否存在冗余/遗漏步骤 |
| Multi-Agent 协同能力评测 | 评测多 Agent 协作场景下的整体任务完成质量和协作效率 | 关注：整体任务成功率（不只是单个 Agent 是否"看起来做对了"）、通信/协调开销、角色分工合理性、是否存在协作中的错误传播/放大 |
| 长期记忆 RAG 评测 | 评测依赖 RAG 支撑的长期记忆系统在检索和利用历史信息上的表现 | 本质上是把"长期记忆"当作一个专门的知识库来做 RAG 式评测：检索召回率/精确率 + 生成阶段的忠实性和正确性 |

#### 8.2 关键问题解答

**Q1：Agent 测评与 LLM 测评有什么区别？**
LLM 测评聚焦模型**单点能力**：语言理解、知识问答、推理、生成质量等，通常是"给一个输入、看一次输出"的静态评测；Agent 测评聚焦**系统级的端到端任务完成能力**：不仅要看最终结果对不对，还要看中间的规划是否合理、工具调用是否正确、多轮交互中记忆是否一致、遇到异常/工具失败时能否恢复，本质上是对"一个包含 LLM、工具、记忆、编排逻辑在内的完整系统"的评测，评测维度更多元，也更接近传统软件测试中的"集成测试/端到端测试"思路。

**Q2：NL2API 的原理是什么？NL2SQL 的原理是什么？NL2Code 的原理是什么？**
三者原理本质相通：都是让模型学习"自然语言语义"到"目标结构化语言（API 调用/SQL/代码）"之间的映射，通常依赖模型在预训练/微调阶段见过大量"自然语言描述-结构化表达"的配对数据，推理时结合当前可用的 API 文档/数据库 Schema/代码上下文，生成语法正确、语义符合意图的结构化输出。区别在于目标语言的形式化程度和验证方式不同：NL2API 验证接口和参数是否正确；NL2SQL 可以通过实际执行查询结果比对来验证语义正确性；NL2Code 需要通过单元测试/执行结果验证功能正确性。

**Q3：记忆的存储方法、操作技术有哪些？**
存储方法：向量数据库（存语义记忆，支持相似度检索）、结构化数据库/知识图谱（存实体关系类事实）、KV 存储（存偏好/配置类简单键值信息）；操作技术：**写入（Write）**——决定什么信息值得被记住并结构化存储；**检索（Retrieve）**——根据当前上下文按需取用相关记忆（常结合 RAG）；**更新（Update）**——当新信息与旧记忆冲突时如何合并/覆盖；**遗忘/压缩（Forget/Summarize）**——对过时或低价值信息做归档、摘要或清理，控制记忆规模。

**Q4：长期记忆为什么需要 RAG？**
长期记忆的信息总量通常远超模型上下文窗口容量，不可能把全部历史/知识都塞进 Prompt；RAG 提供了一种"按需检索"机制，只把与当前任务/问题最相关的记忆片段取出注入上下文，既控制了 Token 成本，又避免了无关历史信息稀释模型注意力，因此长期记忆系统在工程实现上通常就是"以过往交互/知识为语料库的一个 RAG 子系统"。

**Q5：单 Agent 规划 CoT 如何测评？**
关注推理链本身的质量而不仅是最终答案：**逻辑连贯性**（每一步是否从上一步合理推出）、**步骤正确性**（中间事实/计算是否准确，可拆解逐步核对）、**最终答案正确率**、以及**是否存在"正确答案但推理过程错误"的偶然正确**现象（这在测评中需要单独识别，因为偶然正确的推理链不代表模型真正掌握了任务）。

**Q6：单 Agent 规划 ReAct 如何测评？**
除了看最终任务是否完成，还要评测**过程质量**：Thought 是否准确反映了当前应该做的事、Action 中工具选择是否恰当、参数是否正确、遇到 Observation 异常（工具报错/返回空结果）时是否能合理调整策略而非"一条路走到黑"、完成任务所用的**步数/工具调用次数**是否合理（效率维度，步数过多说明规划能力弱或存在无效试探）。

**Q7：Multi-Agent 系统测评方法有何不同？**
相比单 Agent 评测，Multi-Agent 评测需要额外关注：① **协作效率**——完成同等任务所需的总通信轮次、总耗时、总 Token 成本是否合理；② **分工合理性**——各 Agent 是否在其专长范围内被合理调用，是否存在职责重叠或空白；③ **错误传播与放大**——某个子 Agent 的错误是否被下游 Agent 无条件采信并放大，是否有交叉校验/审查机制及其有效性；④ **整体 vs 局部**——需要设计"端到端任务成功率"作为核心指标，避免只看某个子 Agent"看起来回答得不错"就误判整体系统质量。

#### 8.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：只用"最终答案是否正确"作为 Agent 评测的唯一标准——面试官会追问"过程指标"的价值，正确答案配合错误/低效的过程在生产环境中往往意味着高成本、不稳定、难维护。
- **高频追问**：如何评测 Agent 面对"工具调用失败"这类异常场景的鲁棒性？（构造专门的**故障注入测试集**：模拟工具超时、返回错误码、返回格式不符预期等场景，观察 Agent 是否有重试/降级/上报机制，而不是简单假设成功后直接进入死循环或输出编造结果。）
- **测试测开视角**：这是本模块的核心——Agent 评测应当遵循"能力维度拆解 + 过程与结果并重 + 异常场景专项覆盖"的测试设计原则，与传统软件测试中"单元测试关注函数内部逻辑、集成测试关注模块协作、端到端测试关注用户可感知结果"的分层思路高度一致，可以类比讲述以体现测开背景优势。

---

### 模块 9：LLM 大模型测评体系

> 【修正】原资料该部分编号为 1、2、3、5、6、9、10、11、12、13、15（跳过 4、7、8、14），推测为整理时的编号遗漏，未发现内容实质缺失，此处按逻辑顺序重新整理呈现，不再保留原跳号编号。

#### 9.1 核心概念表

| 概念 | 一句话解释 | 面试要点 |
|---|---|---|
| 大模型测评的策略价值 | 测评结果服务于多种决策场景 | 六大价值场景：**发布决策**（模型是否达到上线门槛）、**底座选型**（多个候选基座模型对比选优）、**创新改进**（定位当前模型短板指导迭代方向）、**学术研究**（能力边界探索）、**第三方测评**（独立/权威机构的公信力评估）、**行业准入**（满足监管/行业合规门槛要求） |
| 被测对象三层结构 | 大模型系统的测评对象可以分层看待 | ① **模型本身（Base/Chat Model）**：纯模型能力，脱离具体应用场景；② **作为代理的应用（Agent 应用）**：模型+工具+记忆+编排构成的具体 Agent 系统；③ **全栈 AI 产品（Product）**：包含前端交互、业务逻辑在内的完整用户可感知产品，测评还需纳入用户体验维度 |
| 测评能力框架 | 支撑规模化测评的方法论和工程能力集合 | 包含：测评基线数据**自动生成**能力、**专项测试技术**（针对特定能力如安全/工具调用的专门测试方法）、**工程工具与流程规范**、**大模型系统集成验证流程**（测试策略设计 → 测试数据集构建 → 测试执行 → 结果分析 → 评估报告，五个标准活动） |
| 测试数据集核心抓手 | 构建高质量测评数据集需要把握的三个核心要求 | **覆盖全**（覆盖各类任务/场景/难度/边界情况）、**数量多**（保证统计意义上的可信度）、**测评快**（支持快速迭代和自动化执行，不能每次都依赖纯人工评估） |
| 测评平台质量属性——功能性 | 衡量模型输出"好不好"的功能维度 | 准确性、相关性、可用性、信息量、一致性、泛化性、逻辑性 |
| 测评平台质量属性——可靠性 | 衡量模型表现"稳不稳"的维度 | 稳定性（同类输入表现是否一致）、鲁棒性（对抗噪声/攻击性输入的抗干扰能力）、校准与不确定性（模型给出的置信度是否真实反映其正确概率，即 Calibration） |
| 安全合规维度 | 大模型上线前必须评测的合规相关属性 | 隐私保护、安全（防止生成危险内容）、防攻击（防 Prompt 注入/越狱攻击）、毒性（Toxicity，是否输出冒犯性内容）、无害内容、合规（满足法律法规要求）、公平性（是否存在群体偏见/歧视性输出） |
| 性能效率维度 | 大模型工程性能相关指标 | 并发能力、**首 Token 时延（TTFT）**、涉及训练性能（如训练吞吐/收敛速度）和推理性能（如生成吞吐 TPOT、端到端响应时间） |
| STA 测试设计（Scenario Task Ability） | "场景任务能力"测试设计，是构建测评基线大纲的关键方法 | 从真实业务场景出发反推需要覆盖的任务类型和能力项，自底向上构建有代表性、贴近实际使用分布的测评基线，而非仅依赖学术通用基准 |
| 数据回流 | 从生产环境的真实用户交互中收集数据，反哺测评集构建和模型优化的机制 | 构成"业务运营 → 数据回流 → 测评/训练集更新 → 模型优化 → 上线服务用户"的**数据飞轮（Data Flywheel）**闭环，是测评体系保持"与真实世界同步"的关键环节 |
| 裁判模型（LLM-as-a-Judge） | 用规则或（通常更强的）另一模型自动分析、判定被测模型输出质量的方法 | 核心价值：大幅降低人工评测成本、提升评测效率和规模化能力，常见风险是裁判模型自身的偏差（如偏好更长的回答、位置偏差）需要专门校准和交叉验证 |
| 绝对测评 | 用参考标准答案（Ground Truth）对比被测输出，计算量化质量指标 | 常用指标：正确率/准确率（Accuracy）、**ROUGE**（侧重召回，常用于摘要任务）、**BLEU**（侧重精确率，常用于翻译任务）、F1 等 |
| 相对测评 | 没有唯一标准答案时，通过比较不同模型/不同版本的输出质量做相对优劣判断 | 常用方式：成对比较（Pairwise Comparison）+ 质量打分/胜率统计、类 **ELO 评分**机制对模型排名 |

#### 9.2 关键问题解答

**Q1：什么是 LLM 模型测评？**
LLM 模型测评是通过系统化设计的测试数据集、测评指标和测评方法，对大语言模型在准确性、可靠性、安全合规性、性能效率等多维度能力进行量化或半量化评估的工程与研究活动，服务于模型发布决策、选型对比、迭代改进等多种目的。

**Q2：大模型测评能力框架包含哪几部分？**
测评基线数据自动生成能力、专项测试技术、工程工具与流程规范、大模型系统集成验证流程（测试策略设计、测试数据集构建、测试执行、测试结果分析、测试评估报告五大活动）。

**Q3：大模型系统的被测对象有哪些层级？**
模型本身、作为代理的应用（Agent 应用）、全栈 AI 产品，三层递进，测评的粒度和维度逐层扩展（见 9.1）。

**Q4：大模型测评的核心质量属性包括哪些方面？**
功能性质量属性（准确性、相关性、可用性、信息量、一致性、泛化性、逻辑性）+ 可靠性质量属性（稳定性、鲁棒性、校准与不确定性），再加上安全合规和性能效率两大专项维度，共同构成完整的质量视图。

**Q5：大模型测评的专项技术包括哪些方面？**
对应模块 8 中的工具调用能力评测（NL2API/NL2SQL/NL2Code）、记忆能力评测、规划能力评测、Multi-Agent 协同能力评测、长期记忆 RAG 评测；也包括安全类专项测试（红队测试/越狱测试）、鲁棒性专项测试（对抗样本/噪声输入测试）。

**Q6：业界的主要大模型测评基准有哪些？**【补充，属常见通识，未在资料中展开】
通用能力类：MMLU（多学科知识与推理）、C-Eval/CMMLU（中文能力）；数学与推理类：GSM8K、MATH；代码类：HumanEval、MBPP；对话/指令遵循类：AlpacaEval、MT-Bench（常用裁判模型打分）；安全类：各类红队/越狱测试基准。面试中如被追问具体基准细节，建议诚实说明"记忆可能不是最新版本，建议以官方最新榜单为准"，体现严谨态度。

**Q7：模型的不确定性与校准指的是什么？什么是大模型的鲁棒性？**
**校准（Calibration）**：指模型输出的置信度/概率是否真实反映其答案正确的可能性——如果模型说"我有 90% 把握"，理想情况下这类回答在长期统计中确实应有约 90% 是正确的，校准差的模型会"该自信时不自信、不该自信时过度自信"；**不确定性（Uncertainty）**通常指模型对自己给出答案的把握程度的量化表达。**鲁棒性（Robustness）**指模型面对输入的微小扰动（同义改写、噪声、对抗性构造的输入）时，仍能保持稳定、正确输出的能力，鲁棒性差的模型可能因为一个无关紧要的措辞变化就给出截然不同甚至错误的答案。

**Q8：大模型测评的基本流程包括哪些活动？**
测试策略设计（明确测评目标、范围、维度）→ 测试数据集构建（覆盖全、数量多、可快速执行）→ 测试执行（自动化跑分/人工评估/裁判模型评估）→ 测试结果分析（指标统计、错例归因）→ 测试评估报告（面向发布决策/选型决策等不同受众输出结论）。

**Q9：测评中测试数据集的关键作用是什么？**
测试数据集是整个测评活动的"地基"：其覆盖面直接决定测评结果能否反映模型在真实使用分布下的表现（覆盖不全会导致"测得好但用起来翻车"）；其规模和质量决定统计结论的可信度；数据集本身若被污染（与训练数据重叠）会导致评测结果虚高失真，因此数据集的构建、维护、去污染是测评体系的核心工作之一。

**Q10：测试数据生成的基本原理是什么？**
一般结合人工设计（专家基于业务场景/已知失败模式手写高价值用例）和自动化/半自动化生成（利用模板+参数化批量生成、利用强模型辅助生成候选用例再人工筛选校验、从生产环境数据回流中提炼真实分布的用例）两种方式，兼顾覆盖面的"广度"（自动化批量生成）和针对性的"深度"（专家设计的边界/对抗用例）。

**Q11：什么是绝对测评和相对测评？**
绝对测评依赖预先准备好的标准答案（Ground Truth），用量化指标（正确率、ROUGE、BLEU、F1 等）直接衡量被测输出与标准答案的接近程度，适合有明确"正确答案"的任务（如翻译、摘要、问答）；相对测评不依赖唯一标准答案，而是通过多个模型/版本的输出两两比较，用胜率、质量打分或 ELO 排名的方式判断相对优劣，适合开放式、主观性较强、"好坏"难以用单一标准答案定义的任务（如创意写作、开放域对话质量）。

**Q12：评分规则与错例分析如何进行？**【补充，结合评测工程通识展开】
评分规则需要提前明确、可复现（无论是规则打分还是裁判模型打分，都应有清晰的评分标准/Rubric，避免模糊主观）；错例分析通常按"错误类型"分类归因（如事实性错误、指令理解偏差、格式错误、工具调用错误、安全类问题等），统计各类错误的占比和典型案例，用于反哺后续的数据补充、Prompt/Harness 优化或模型迭代方向决策，而非仅仅产出一个笼统的总分。

**Q13：裁判模型在结果分析中的作用？**
裁判模型（LLM-as-a-Judge）在无法/难以人工大规模评估或缺乏标准答案的场景下，承担"自动化打分员"的角色：对被测模型的输出按照给定的评分维度（相关性、有用性、安全性等）打分或进行成对比较，大幅提升评测的效率和可扩展性，使得高频次、大规模的回归评测成为可能；同时裁判模型自身也存在偏差风险（如偏好更长/更"礼貌"的回答、位置偏差），因此实践中常需要用人工抽样校验裁判模型的评分一致性，或采用多个裁判模型交叉验证。

**Q14：数据回流在测评中的意义是什么？**
数据回流把"模型上线后在真实生产环境中的实际表现"重新收集回来，用于发现测评集未覆盖的新失败模式、构建更贴近真实用户分布的测评/训练数据，从而形成"业务运营 → 数据回流 → 测评/训练集更新 → 模型迭代 → 更好地服务业务"的数据飞轮闭环。没有数据回流，测评体系容易与真实世界"脱节"，长期依赖静态的学术基准会导致"刷榜但不好用"的问题。

**Q15：大模型测评的工程工具主要有哪些？**【补充，属常见工程实践通识】
自动化测评框架/平台（用于批量执行测试用例、计算指标、生成报告）、向量检索/相似度计算工具（支撑语义类指标计算）、裁判模型调用与打分管理系统、数据集版本管理与标注平台、性能压测工具（评测并发/时延/吞吐）、以及用于安全测评的红队/越狱测试工具集。

#### 9.3 高频追问 / 易错点 / 测试测开视角

- **易错点**：把"绝对测评"等同于"更客观、更好"——绝对测评依赖标准答案，本身在开放式任务上"标准答案"的定义可能就存在主观性和局限性，不能简单认为绝对测评一定优于相对测评，两者是互补关系，应根据任务性质选择。
- **高频追问**：ROUGE 和 BLEU 的区别？（BLEU 最初为机器翻译设计，侧重**精确率**（生成内容中有多少 n-gram 出现在参考答案中）；ROUGE 更常用于摘要任务，侧重**召回率**（参考答案中的内容有多少被生成内容覆盖到），两者都是基于 n-gram 重叠的传统指标，对语义相似但字面不同的表达不够友好，近年也常辅以基于 Embedding 的语义相似度指标或裁判模型打分作为补充。）
- **测试测开视角**：这是本模块及全笔记的落地核心——作为测开，应当具备**独立设计测评框架**的能力：明确被测对象层级 → 设计覆盖全/数量多/可快速执行的测试集 → 选择合适的绝对/相对测评方法和裁判模型方案 → 建立数据回流机制持续补充测试集 → 产出可支撑发布决策的量化报告，这一整套闭环能力正是该岗位区别于普通功能测试岗位的核心竞争力。

---

## 三、面试速答清单（可直接背诵）

1. **AI ⊃ 大模型 ⊃ LLM**；LLM 是大模型在语言方向的子集，传统 NLP 是任务孤立的旧范式。
2. **LLM 运作原理**：基于 Transformer 的自回归模型，不断预测下一个 token。
3. **预训练 vs 后训练**：预训练学知识和语言能力（成本最大），后训练（SFT+RLHF/DPO）做指令遵循和偏好对齐，一般不显著新增知识。
4. **Scaling Law**：训练时——模型/数据/算力按幂律关系提升效果；推理时——Test-time Compute，让模型多想一会儿换准确率。
5. **Agent = LLM + 感知 + 规划 + 记忆 + 工具调用**。
6. **ReAct** = Thought → Action → Observation 循环；**Reflection** = 做完再自我批判修正。
7. **记忆四分类**：工作记忆（短期）、情景记忆、语义记忆、程序记忆。
8. **MCP（Agent↔工具）、A2A（Agent↔Agent，受控场景）、ANP（Agent↔Agent，开放网络）**三协议服务对象不同。
9. **Multi-Agent 三种架构**：集中编排式（主流）、层级式、去中心化对等式。
10. **Prompt 四要素**：角色、任务、背景、格式/约束。
11. **Prompt Engineering < Context Engineering < Harness Engineering**：一次输入 < 全部可见信息 < 整个工程系统。
12. **上下文压缩**：警惕"Lost in the Middle"，压缩不能丢关键约束，触发点是容量阈值/阶段节点。
13. **RAG 流程**：解析 → 语义分片 → 向量化 → 检索（可混合）→ 重排序 → 增强 → 生成。
14. **RAG vs 微调**：知识时效性/成本/可解释性用 RAG；风格/格式/领域推理模式固化用微调；常组合使用。
15. **混合检索** = 稀疏（BM25）+ 稠密（向量），弥补纯语义检索对精确词/专有名词召回不准的短板。
16. **大模型幻觉**：生成看似合理但缺乏依据/与事实矛盾的内容，RAG 能降低但不能完全消除。
17. **Harness Engineering** 管认知框架（怎么想）、能力边界（能做什么）、模型行为（怎么控制循环和异常）。
18. **Agent 评测 ≠ LLM 评测**：Agent 评测是端到端系统测评，既看结果也看规划/工具调用/记忆的过程质量。
19. **绝对测评**（有标准答案，ROUGE/BLEU/Accuracy）**vs 相对测评**（无标准答案，胜率/ELO）。
20. **裁判模型（LLM-as-Judge）**降低人工评测成本，但本身有偏差风险，需人工抽样校验。
21. **数据回流** = 生产数据 → 测评/训练集更新 → 模型迭代，构成数据飞轮。
22. **DCAI**：模型不变，靠提升数据质量提升系统效果。
23. **ETL 先转后加载**（传统数仓）**，ELT 先加载后转换**（现代云仓库/大数据，更灵活）。
24. **数据清洗修正错误，数据预处理适配模型**。

---

## 四、附录

### 4.1 术语表（统一口径）

| 缩写/术语 | 全称 | 一句话说明 |
|---|---|---|
| LLM | Large Language Model | 大语言模型，大模型的语言方向子集 |
| Agent | AI Agent | 具备感知/规划/记忆/工具调用能力的自主任务执行系统 |
| RAG | Retrieval-Augmented Generation | 检索增强生成 |
| MCP | Model Context Protocol | Agent 与工具/数据源之间的标准化连接协议 |
| A2A | Agent2Agent Protocol | Agent 与 Agent 之间协作通信协议（受控场景为主） |
| ANP | Agent Network Protocol | 开放网络场景下的智能体身份与协作协议 |
| ReAct | Reasoning + Acting | 推理与行动交替的 Agent 执行范式 |
| CoT | Chain-of-Thought | 思维链，引导模型输出中间推理步骤 |
| Reflection | 反思模式 | 执行后自我评估并修正的模式 |
| Harness | Agent Harness | 包裹模型运行的完整工程脚手架 |
| SFT | Supervised Fine-Tuning | 有监督微调 |
| RLHF | Reinforcement Learning from Human Feedback | 基于人类反馈的强化学习 |
| DPO | Direct Preference Optimization | 直接偏好优化 |
| RoPE | Rotary Position Embedding | 旋转位置编码 |
| TTFT | Time To First Token | 首 Token 时延 |
| TPOT | Time Per Output Token | 单 Token 生成耗时 |
| DCAI | Data-Centric AI | 以数据为中心的 AI 研发范式 |
| ETL/ELT | Extract-Transform/Load-Load/Transform | 数据处理流程两种范式 |
| EDA | Exploratory Data Analysis | 探索性数据分析 |
| NL2SQL/API/Code | Natural Language to SQL/API/Code | 自然语言转结构化表达任务 |
| STA | Scenario Task Ability | 场景任务能力（测评基线设计方法） |

### 4.2 关键流程代码块

**ReAct 执行循环（伪代码）**
```
def react_agent(task, tools, max_steps=10):
    context = init_context(task)
    for step in range(max_steps):
        thought, action = model.think(context)          # Thought + 决定 Action
        if action.type == "final_answer":
            return action.content
        observation = execute_tool(action, tools)         # Action -> 执行
        context = update_context(context, thought, action, observation)  # Observation 回填
    return "达到最大步数，任务未完成"
```

**RAG 标准流水线（伪代码）**
```
# 离线阶段：构建知识库
docs = parse_documents(raw_files)          # 文档解析
chunks = semantic_chunking(docs)           # 语义分片
vectors = embed(chunks)                    # 向量化
vector_store.upsert(vectors, chunks)       # 入库

# 在线阶段：检索增强生成
def rag_answer(query):
    dense_hits = vector_store.search(embed(query), top_k=50)
    sparse_hits = bm25_index.search(query, top_k=50)
    candidates = hybrid_merge(dense_hits, sparse_hits)   # 混合检索
    top_chunks = reranker.rerank(query, candidates, top_n=5)  # 重排序
    prompt = build_augmented_prompt(query, top_chunks)   # 增强
    return model.generate(prompt)                        # 生成（含忠实性约束）
```

**LLM 测评基本流程（伪代码）**
```
def evaluate(model, test_set, judge=None):
    results = []
    for case in test_set:
        output = model.generate(case.input)
        if case.has_ground_truth:
            score = absolute_eval(output, case.ground_truth)   # 绝对测评：Accuracy/ROUGE/BLEU
        else:
            score = judge.score(case.input, output, rubric=case.rubric)  # 裁判模型/相对测评
        results.append({"case": case, "output": output, "score": score})
    report = aggregate_and_analyze(results)   # 指标汇总 + 错例归因
    return report
```

### 4.3 常用指标清单

**生成质量类**
- Accuracy / Exact Match（准确率/精确匹配）
- ROUGE（召回导向，常用于摘要）
- BLEU（精确率导向，常用于翻译）
- F1 Score
- Pass@k（代码生成，k 次采样至少一次通过测试）
- Execution Accuracy（NL2SQL/NL2API，执行结果比对）

**检索类（RAG）**
- Recall@K / Precision@K
- MRR（Mean Reciprocal Rank）
- NDCG（Normalized Discounted Cumulative Gain）
- Faithfulness / 忠实性（生成内容是否有据可查）

**Agent / 过程类**
- 任务端到端成功率（Task Success Rate）
- 工具调用准确率（工具选择 + 参数正确率）
- 平均步数 / 工具调用次数（效率指标）
- 关键信息召回率（记忆/压缩场景）

**可靠性与安全类**
- 稳定性（重复请求一致性）
- 鲁棒性（对抗/噪声输入下的表现衰减程度）
- 校准误差（Calibration Error，置信度与实际正确率的偏差）
- 毒性率 / 越狱成功率（安全测评）

**性能效率类**
- TTFT（首 Token 时延）
- TPOT / 吞吐（Tokens/s）
- 并发数
- 端到端响应时延

**相对测评类**
- 胜率（Win Rate）
- ELO 评分
- 裁判模型评分一致性（与人工标注的相关系数）

---

## 五、质量自检说明

- 已覆盖资料中列出的全部关键概念清单与全部问题清单（模块 1-9 逐一对应资料原文顺序）。
- 每个模块均包含"核心概念表 + 关键问题解答 + 高频追问/易错点/测试测开视角"三段式结构。
- 全篇围绕测试测开岗位视角补充了可测性、评测指标、评测集设计、自动化回归、异常/故障注入测试等落地内容。
- 对原资料中的编号跳号（数据工程模块、LLM 测评体系模块）、问题数与实际列表不符（Context Engineering 模块）、术语歧义（"文档解析 MRC"）均已在对应位置标注【修正】，未做主观臆测替换；【补充】内容均为面试常见通识性延伸，未编造具体资料数值或结论。
